# stepwise-eisu

ステップワイズ個別指導のホームページ + 予約/管理システム(GitHub Pages + Google Apps Script + Google スプレッドシート)。

- 全体像・データの置き場所・API・デプロイ手順は **docs/SYSTEM.md** を先に読む。構成を変えたらそのファイルも更新する。
- 画面は `yoyaku/index.html`(生徒)と `kanri/index.html`(先生)の1ファイル完結。サーバーは `gas/Code.gs`(Apps Script の写し。編集後はエディタに貼り付けて新バージョンをデプロイしないと反映されない)。
- 残っている課題・今後の予定は **docs/FUTURE_WORK.md**。作業中に「後で直す」「ここが気になる」と思ったことは、その場でこのファイルに追記する(終わったら「済み」へ移す)。
- 動作確認は名前に【テスト】が付くテスト生徒で行う。本物の生徒の予定・記録・データは触らない(削除系は特に)。
- 先生のパスワードは扱わない(ユーザーが自分で入力する)。OAuth の同意画面はユーザーの明示的な OK なしに進めない。
- GitHub Pages はブラウザが約10分キャッシュする。確認時は `?nc=<任意>` を付ける。
