# ステップワイズ個別指導 システム概要

最終更新: 2026-09-09 / 管理者: 麦倉優輔 (mugilives@gmail.com)

現行の構成・台帳・API・反映手順の正本。作業対象に関係する節を参照する。共通の制約と資料案内は [AGENTS.md](../AGENTS.md)、未完了事項は [FUTURE_WORK.md](FUTURE_WORK.md)。

GAS現行版はv60。授業記録と月間承認・請求、授業形式と同時人数、案内の一括確定、兄弟姉妹の家族アカウント、保護者メール通知に加え、授業情報の集約・実施分請求の家族明細・保護者の既読を公開した。版ごとの反映範囲と確認結果は [6章](#6-更新デプロイ手順)、運用上の残件はFUTURE_WORKを参照する。

案内編集・予定表の時刻順表示・化学の選択・生徒メール・授業記録の保存時公開・次回授業を使う宿題期限は、実GAS検証と本番の構造準備を経て公開した。3〜5章はこの追加実装を含む仕様を記載し、公開状況と確認範囲は [6-3節](#6-3-v49の反映と検証) に集約する。

授業形式・一括確定の仕様は [5-2節](#5-2-授業形式と一括確定)、家族認証・通知は [5-3節](#5-3-家族アカウントと保護者通知) に集約する。`deliveryMode` 列・`acceptWrites`・`family*` シートはv48の反映時に準備済み。

<a id="local-teacher-workflow"></a>

## 授業運用の追加機能（v50・2026-09-08）

以下はGAS v50・Pagesへ反映済みの追加仕様。公開・検証結果は [6-4節](#6-4-v50の反映と検証) に集約する。旧リンクを保つため、この節のアンカーは維持する。

- 管理ホームと生徒の予定画面に、JSTで前日以前の確定授業（`booked`）かつ実施未登録の件数・対象授業・実施登録への操作を表示する。授業記録の有無とは独立し、古い授業も件数制限で除外しない。時間経過による自動実施登録はしない。実施しなかった授業は既存の解除・取消操作で扱い、承認・請求制御を維持する。
- 生徒ページを「予定・宿題」「成績・面談」「月間計画・請求」「生徒設定」に分離。形式・連絡先・料金既定値・保護者認証は設定へ移し、家族設定への入口も置く。家族データの正本は既存の家族管理のまま。
- `kanriStudent` の `section` は `overview/settings/progress/billing`。省略時は従来の全項目応答を保つ。画面は開いた区分を取得し、通常の保存も同じ区分の最新データを返す。キャッシュは生徒IDと区分で分離する。予定は未来の授業を全件・過去は直近20件、実施未登録は別一覧で保持する。
- 翌日以降の授業の入口は「授業準備」。当日から記録入力を表示する。準備は先生専用で保存し、公開記録への自動転記をしない。保存先と競合・復旧の条件は [授業サイクル仕様](LESSON_CYCLE_PHASE1_SPEC.md#local-lesson-preparation) を参照する。
- 生徒画面の新規登録は「授業可能日時」に統一し、開始・終了時刻の範囲を登録する（API上は `kind=ok`）。ホーム内でも登録できる。授業形式は生徒設定から引き継ぎ、希望の種類・形式の選択欄は表示しない。範囲すべての授業を希望・確定する意味ではない。従来の日時指定データとAPI互換性は保持する。画面の「授業の長さ」欄と「空き状況を確認」の事前確認は 2026-09-11 に外し、「登録する」で直接保存する。保存時にGASが空き状況を判定して `availability` に記録し、満員でも登録でき、先生側には送信時の要調整状態を表示する。単日・複数日とも対応し、形式は生徒設定から引き継ぐ。授業の長さは画面から送らず、GASの既定値（`kind=ok` は60分）で判定・保存する。日時指定はその区間全体、幅のある希望は中に連続した授業時間を確保できるか確認する。返事待ちの案内、確定授業、変更処理中の移動先、先生の休み・本人の授業不可を考慮する。他生徒の氏名・ID・予定明細は返さない。
- 生徒API `wishAvailability` は本人の `k` と `date` または `dates`（最大20日）、`kind/start/end/min/deliveryMode` を受け、日別の `available/full/unavailable/unknown` と受付できる開始時刻の例を返す。希望は席を確保せず、案内・確定時の既存チェックを維持する。送信時に再判定し、`availabilitySeen` を送った場合のみ状態変化を照合する（現在の生徒画面は送らない）。
- `wishes` は既存8列を維持し、`deliveryMode/duration/availability` を末尾に追加。旧行は形式・分数・状態を推測補完しない。新規希望は必要な分数と送信時の状態を保持し、案内フォームへ形式と分数を引き継ぐ。希望内容が同一の再送は重複登録せず、別のメモへの変更を黙って保存済み扱いにしない。日時・形式が未設定の旧クライアントでは入力確認が必要になる場合があるため、GASと画面をまとめて反映する。
- 対面授業の「同時受講の記録」から2人を同じページで扱う。前回記録・準備・宿題・今回の記録を並べ、狭い画面では縦に配置する。保存・公開・再試行は生徒別で、片方の保存で他方の入力欄を描き直さない。詳細は [授業サイクル仕様](LESSON_CYCLE_PHASE1_SPEC.md#local-paired-records)。
- 一覧の記録状態をリクエスト内で索引化し、記録・下書きの書き込み時に破棄する。POST応答の `timings` は `lockMs/schemaMs/operationMs`。管理画面の開発者コンソールには操作名と総時間・サーバー時間のみ記録し、本文・ID・認証情報は出さない。共通ロック・版チェック・通知処理の順序は維持する。

ローカルの模擬台帳（テスト生徒・未来の確定授業100件、成績等は空）で、`kanriStudent_` 内のRange.getValues呼び出しは全項目15回、予定10回、設定3回、成績1回、請求7回。これは実GASの待ち時間や通信量の測定ではない。予定は従来60件で切れていた未来の授業も返すため、件数によって応答サイズは増える。実環境の単発計測は6-4節を参照。旧版との同条件比較は未実施で、改善秒数は断定しない。

ローカル検証は構文チェックと385件の回帰テストを通過（383件の一括確認後、追加した2件を含む対象テストを確認）。準備の非公開・所有者分離・版競合・中断再送、実施未登録の抽出、画面分割後の設定・請求操作、希望時の定員・休み・非公開情報の除外・状態変更、2人の独立した入力・保存・再試行を含む。実際のブラウザでの表示・実GASでの検証は含まない。

公開時には既存の反映手順に従い、予約台帳の `lessonPreparations` 新規シート、`wishes` の末尾3列追加と既存データ保持を確認する。スキーマキャッシュキーは `schemaOk19`。本番の構造準備・実GAS検証・先生と本人の画面確認を実施した。

## 1. 一言でいうと

- 画面(HTML)は **GitHub Pages**、ロジック(API)は **Google Apps Script**、データは **Google Drive のスプレッドシート2つ** にあります。
- 実生徒の業務データは2台帳、成績票などの原資料はDriveに保存する。復旧にはコード・GAS設定・原資料も必要なので、台帳のコピーだけでシステム全体のバックアップが完了したとは扱わない。

```
生徒のスマホ       ──► https://www.stepwise-education.jp/yoyaku/ (生徒ページ)
保護者のスマホ     ──► https://www.stepwise-education.jp/hogosha/ (保護者ページ)
先生のスマホ・PC   ──► https://www.stepwise-education.jp/kanri/  (管理画面, PWA)
        │  fetch(POST, JSON)
        ▼
Google Apps Script Web アプリ (/exec)  … gas/*.gs が本体
        │
        ├─ スプレッドシート「ステップワイズ予約システム」 … 予定・生徒・設定
        ├─ スプレッドシート「塾管理台帳」                 … 台帳・成績・模試・入金・面談
        ├─ Google カレンダー(確定授業の写し)・Gmail(通知メール)
        └─ Drive フォルダ「ステップワイズ塾」              … 成績票PDF など元ファイル
```

## 2. データの置き場所

| 置き場所 | 何があるか | 備考 |
|---|---|---|
| GitHub `mugilives-max/stepwise-eisu`（public、main / root を Pages 配信） | `index.html` ホームページ、`yoyaku/index.html` 生徒マイページ、`hogosha/index.html` 保護者ページ、`kanri/index.html` 管理画面、`gas/*.gs` サーバーコードの写し、`docs/` 文書 | 作業先は [AGENTS.md](../AGENTS.md)。公開確認は `?nc=適当な値` でキャッシュを避け、配信内容を照合する |
| Google Apps Script（予約システムに紐づくコンテナバインド） | 実行中のコード。WebアプリURL・デプロイIDは管理HTMLと `assets/portal.js` の `API` 定数を参照 | [プロジェクト](https://script.google.com/home/projects/1vlfS4thMpRgV0WbHXZ8joFLzZBMmoROdopAJXK6JEIQWplokSgvgo619/edit)。Script IDはこのURLの `/projects/` と `/edit` の間。反映は6章 |
| Drive: スプレッドシート「ステップワイズ予約システム」 | 生徒一覧と専用リンクコード、授業枠、授業できない日、希望日程、共有予定、月の授業回数(計画)と保護者承認、宿題・持ち物、先生ログイン情報(ハッシュ)、操作ログ | 場所: マイドライブ/ステップワイズ塾/ |
| Drive: スプレッドシート「塾管理台帳」 | 生徒台帳、成績推移、模試、入金管理、面談記録 | 同上。ID は `Code.gs` の `LEDGER_ID` |
| [Drive フォルダ「ステップワイズ塾」](https://drive.google.com/drive/folders/1DbCfH9j8BDtFsbe6iXpsUyCu4YURkBVG) | `01_生徒/<生徒>/`(成績票PDFなど)、`02_契約・規約`、`03_教材`、`04_経理`、`99_アーカイブ` | Drive for Desktopのマウント先・稼働状態は環境により異なる。必要なら接続中のDriveから参照 |
| 生徒の端末 | ブラウザに専用リンク、表示キャッシュ、保護者セッションが残る | 業務データの正本ではない。認証情報の扱いは [PARENT_AUTH.md](PARENT_AUTH.md) |

## 3. スプレッドシートの中身

### 3-1. 「ステップワイズ予約システム」(アプリ用。列名は英語)

| シート | 列 | 意味 |
|---|---|---|
| config | key, value | 設定と先生のログイン情報。`passHash`/`passSalt`(パスワードのハッシュ)、`adminToken`/`adminTokenExp`(ログイン中トークン。1か所のみ有効)、`teacherEmail`、`calendarSync`、`emailNotify`、`reset*`(パスワード再設定コード)、`failCount`/`lockUntil` |
| parents | 列順・保存仕様は [PARENT_AUTH.md](PARENT_AUTH.md#認証と保存) | 生徒別の保護者認証（v45追加）。旧students.parentToken/parentExpは使用しない |
| students | id, name, active, email, code, rate30, monthly, parentToken, parentExp, deliveryMode | 生徒。`code` が専用リンク(`/yoyaku/?k=code`)の鍵。`rate30` は30分単価。`monthly` は旧固定月謝の証跡列で、新規の定額設定には使わない。`active=false` は停止中。10列目の `deliveryMode` は新規案内に使う普段の形式 |
| slots | id, date, start, min, status, studentId, done, eventId, meetUrl, subject, req, deliveryMode | 授業枠。`status` は open(空き)/offered(案内中=承認待ち)/booked(確定)。`done=true` で実施済み。`eventId`/`meetUrl` はカレンダー連携。`req` は取消依頼のJSON。12列目の `deliveryMode` はその授業の形式 |
| acceptWrites | id, studentId, requestId, slotsJson, completedJson, status, notificationState, createdAt, updatedAt, lastError | 単件・一括確定の保存と再送の正本。途中処理は同じ生徒・選択内容・処理IDで復旧する |
| offerEdits / slotChangeNotices | 正確な列順は `Scheduling.gs` / `Code.gs` の定数 | 案内・形式変更の再送、取消と通知の保存復旧 |
| studentEmails / studentEmailOutbox / studentEmailPrefs | 正確な列順は `StudentEmail.gs` の定数 | 生徒メールの受信確認、期限付きの確認情報、通知の送信状態。生徒認証や家族認証とは別 |
| familyAccounts / familyLinks / familyChallenges / familyOutbox | 正確な列順は `gas/FamilyPortal.gs` の定数。認証仕様は [PARENT_AUTH.md](PARENT_AUTH.md) | 家族アカウント、先生が確認した子どもの紐付け、メール確認・再設定の記録、保護者通知の送信記録。既存 `parents` と独立 |
| blocked | id, studentId, date, note, start, end | 生徒の「授業できない日」。start/end が空なら終日、入っていればその時間帯だけ |
| teacherOff | id, date, note, start, end | 先生の休み。1行=1日。start/end が空なら終日、入っていればその時間帯だけ。管理画面のホーム(日付タップ)か授業ページから登録。生徒にはメモを見せない |
| wishes | id, studentId, date, start, end, note, createdAt, kind | 生徒の希望日程。`kind` は want(この日時に授業をしたい)/ok(この時間帯のどこかで) |
| events | id, studentId, date, dateTo, title, createdAt, kind | 生徒が共有した予定(大会・見学など)。`kind=test` はテスト・模試(マイページでカウントダウン表示) |
| plans | id, studentId, ym, subject, count, status, proposedAt, approvedAt, approvedVia, memo | 月の授業回数(計画)。`ym` は `YYYY-MM` か `default`(毎月の既定)。`status` は draft/proposed/approved/declined。表示互換用の承認欄を残すが、承認の有効性はmonthAgreementsと月別回数の一致で判定 |
| tasks | id, studentId, type, title, due, createdAt, createdBy, doneAt, sourceRecordId, sourceItemId, sourceRevision, withdrawnAt, dueMode, dueSubject, dueAfter, dueTime | 宿題・持ち物。授業記録由来の課題は元の記録・項目・版を保持し、取り下げても履歴と完了状態を消さない |
| lessonRecords / lessonPrivateNotes / lessonReportDrafts / lessonWrites / lessonPublicSnapshots | 列・再送・復旧仕様は [授業サイクル仕様](LESSON_CYCLE_PHASE1_SPEC.md) | 指導記録、先生だけのメモ、未公開の報告、保存処理と公開済み本文の版 |
| monthAgreements / approvalEvents | 正確な列順は `gas/BillingApproval.gs` の定数 | 生徒・月ごとの回数・料金・承認版の正本と、承諾・請求・訂正の履歴 |
| log | time, message | 操作ログ(日本語1行) |
| mcpLog | time, requestId, client, op, target, params, result, ms | MCP(ChatGPT/Codex)からの呼び出し記録 |

### 3-2. 「塾管理台帳」(人が見る台帳。列名は日本語)

| シート | 列 |
|---|---|
| 生徒台帳 | 生徒ID, 氏名, ふりがな, 学年, 学校, 保護者名, 保護者連絡先, メール, 入塾日, 状態, 科目, 単価(30分), 月謝, 備考 |
| 成績推移 | 日付, 生徒ID, 氏名, テスト名, 科目, 点数, 満点, 偏差値, 順位, 備考(1行=1科目) |
| 模試 | 日付, 生徒ID, 氏名, 模試名, 回, 学年, 国語, 国語偏差値, 数学, 数学偏差値, 社会, 社会偏差値, 理科, 理科偏差値, 英語, 英語偏差値, 3教科, 3教科偏差値, 5教科, 5教科偏差値, 3教科順位, 5教科順位, 受験者数, 志望校判定(" / "区切り), 資料URL(成績票PDF。先生のみ表示), 備考(1行=1回分) |
| 入金管理 | 年月, 生徒ID, 氏名, 請求額, 請求日, 入金日, 入金方法, 状態, 備考, 請求ID, 承認版, 料金方式, 確定単価(30分), 確定月謝, 実施分数, 実施回数, 実績JSON, 取消日時, 取消理由, 処理ID, 入金版 |
| 面談記録 | 日付, 生徒ID, 氏名, 相手, 方法, 内容, 次のアクション |

`生徒ID` は「ステップワイズ予約システム」students シートの `id` と同じ値で、2つのファイルをつないでいます。

構造変更では列名・列順・ID・行番号の参照を確認する。`config`、`slots`、`plans`、`students.code`、入金状態、ログ、Script Propertiesのキーは管理画面または対応API/専用関数から更新する。台帳の直接編集が必要な場合も、成績・面談など行番号で参照する処理と既存データへの影響を確認してから行う。

## 4. 画面

| URL | 誰が | 中身 |
|---|---|---|
| `/yoyaku/?k=<code>` | 生徒(LINEで専用リンクを配布) | マイページ(ヘッダー左上は「〇〇さんのマイページ」)。タブ: ホーム(予定表(登録不可の日・時間帯も表示)・見出し「予定の編集」の下に選んだ日の予定(＋で授業可能日時・授業できない日・予定の共有を登録。登録済みの授業可能日時の取消・授業できない日の解除・予定の削除も同じ場所)。＋を押すと「手動で予定入力」(3ボタン)と「文章で自動入力」が出る(AI 変換→確認→登録。鍵設定時のみ))・やることリスト(折り畳み、既定は開。テストまでの日数・宿題)・授業登録(案内があるときだけの折り畳み。確定/再調整)・授業計画(折り畳み。「案内」=保護者の承認待ちの月の計画を科目ごとの行で、「実施計画」=承認済みの月の計画と実施・予定・残り回数。`state.planMonths`＝今月と来月)。成績票・振り返り／先生への連絡の欄は「設定」タブにだけ表示(2026-09-11))/ 成績(模試・成績推移)/ 授業の記録（科目＋種類ごとのフォルダ。公開された授業記録と実施済みの授業を日付順に） / 設定（メール通知の登録・受信確認・解除・項目別オン/オフ、成績票・振り返り／先生への連絡） / 保護者(パスワード制。今月の授業・お支払い・授業回数の承認) |
| `/yoyaku/#family` | メール確認を終えた家族の保護者 | 1回のログインで先生が紐付けた子どもを切り替え、予定・料金承認・請求を確認する。子ども別の専用リンクを持ち替える必要はない |
| `/kanri/` | 先生(メール+パスワードでログイン。PIN は廃止、再設定は登録メール宛の6桁コード) | ホーム(取消依頼・共有予定・希望、今日/今週、全体の予定表、生徒カード)/ 授業(案内フォーム・承認待ち・カレンダー・NG日・実施記録と請求文面)/ 生徒(追加・停止中)/ 生徒カルテ `#s=<id>`(予定表、基本情報、リンク設定、今月の授業と請求、月の授業回数と承認、成績推移、模試、今後の予定、履歴、入金、面談)/ 設定 |

保護者は先生と別の専用認証を使用する。利用の流れ・認証仕様は [PARENT_AUTH.md](PARENT_AUTH.md)。月間承認の本運用と契約条項の確定は認証実装とは別の課題。

先生画面では送信済み案内の日時・科目・形式を同じ授業枠のまま修正できる。ホーム予定表では時刻付きの休みと授業を開始時刻順に混在表示する。化学は案内・月間計画・一般成績で選べる（模試の5教科列は変更しない）。生徒画面では案内を選択して一括確定し、先生が保存した授業記録を閲覧できる。

## 5. API(Apps Script)

- 呼び出しは `POST /exec` に JSON。`action` で分岐。生徒側は `k`(専用リンクのコード)で本人確認、先生側は `action:"admin"` + `token`(ログイン時に発行)+ `op`。
- 生徒側 action: accept / decline / cancelReq / wish / unwish / wishMany / eventAdd / eventAddMany / eventDel / block / unblock / blockSet / taskAdd / taskDone / taskDel / grades。保護者actionと先生のコード発行opは [PARENT_AUTH.md API](PARENT_AUTH.md#api)。
- 予約・通知APIには、生徒側 `acceptMany` / `studentEmail*`、先生側 `setDeliveryMode` / `setSlotDeliveryMode` / `editOffered` / `studentEmailNotifications` / `studentEmailRetryNotification`、家族認証・子ども切替・通知管理の `family*` がある。予約の契約は5-2節、家族APIの詳細は [PARENT_AUTH.md](PARENT_AUTH.md) に置く。
- 先生側 op: state / offer / deleteSlot / unbook / toggleDone / finishOffered(返事がないまま日付が過ぎた案内を確定・実施済みにする) / addStudent / setEmail / setFee / newCode / addBlock / delBlock / addOff / delOff(先生の休み) / hideStudent / changePass / resolveCancel / delWish / delEvent / planSet / planPropose / planApproveTeacher / taskAdd / taskDone / taskDel / kanriDashboard / kanriStudent / kanriSaveProfile / kanriAddGrade / kanriAddExam / kanriAddPayment / kanriSetPaid / kanriAddMeeting / kanriDeleteRow / kanriSetActive / logout。ログイン前: login / setupAccount / resetRequest / resetConfirm
- 確定授業の取消は生徒からの「依頼」で先生が承認（締切は授業の24時間前 `CANCEL_DEADLINE_H`）。月間承認と請求の制御は次節。
- 授業記録の先生専用op・返却範囲は [授業サイクル仕様](LESSON_CYCLE_PHASE1_SPEC.md)。通常のカルテ・予定・MCPには内部メモや下書き本文を含めない。
- エディタから手で実行する関数: `setup`(初回のシート作成)、`resetTeacherLogin`(先生ログイン初期化)、`kanriSelfTest`、`mcpRotateKey`(MCP 用キーの発行・更新)、`mcpDisable`(MCP 停止)、`mcpEnableWrites` / `mcpRestrictWritesToTest`(MCP 登録の範囲切替)。
- MCP 用の入口: `action:"admin"` + `mcpKey`(Script Properties の `MCP_KEY`)。実行できる op は `MCP_READ_OPS`(mcpPing / mcpStudents / mcpSchedule / mcpStudent / mcpPending / mcpBilling / mcpTeacherOff / mcpWishes)と `MCP_WRITE_OPS`(v52〜: mcpOfferLessons / mcpAddTeacherOff / mcpAddStudentNg / mcpAddStudentWishes。項目ごとの検証と結果、既存と同じ日時は登録済み扱い。v54〜: mcpInboxClaim / mcpInboxResolve と閲覧の mcpInboxList。連絡欄の処理はシート `contactProcessing` に記録)のホワイトリストのみ。書き込み範囲は Script Properties `MCP_WRITE_SCOPE`(test/all。エディタの `mcpEnableWrites` / `mcpRestrictWritesToTest`)。呼び出しは `mcpLog` シートに記録。返却値に専用リンクコード・メール・トークンは含めない。仕様は [MCP_OPERATIONS.md 2章](MCP_OPERATIONS.md#2-いま使える機能)。

### 5-1. 月間承認と請求

**運用方針との相違（2026-09-09確認）**: 固定月謝は存在せず、実施分だけを請求する。以下の月謝関連の記述は現在のコードに残る挙動であり、採用する運用ルールではない。固定月謝分岐の廃止と、保護者宛ての兄弟合算・子ども別明細の請求書は [Future Work](FUTURE_WORK.md#家族合算の請求) に記録した未実装の修正事項。

- `plans` は科目別回数、`monthAgreements` はその月に提示した回数・30分単価・月謝・承認版の正本。`default` は新しい月の入力補助で、承認の代わりにはならない。標準料金の変更は合意済み月の料金を変えない。
- 先生画面の `planSet` は、保存対象の生徒・月の最新 `expectedRevision` を送る。未登録月は0、`default` は版指定なし。既存月の版を取得できていない画面からは保存せず、更新を促す。
- 通信失敗時は画面を更新して最新状態を確認し、同じ内容を保存する。計画行の保存後に止まった未提案の下書きは、その月に保持している料金・版を変えず、不足した回数の写し・表示状態・監査履歴を補完する。`monthAgreements` が残る月は、過去月の最後の計画行を削除してもAPIの月一覧に残し、最新版から再送・再計画できる。
- 案内は承認前でも出せる。生徒の予約確定、先生の実施記録、期限切れ案内の実施処理、請求は、本人・対象月・科目の承認と回数枠をサーバーで確認する。確定済み枠を1回ずつ数え、実施済みとの二重計上をしない。追加回数や月の料金変更は再提案・再承認が必要。
- `planPropose` は `studentId,ym,expectedRevision,rate30,monthly`。過去月は当時の単価を明示する。`monthly` は0のみで、旧固定月謝の承認は再提案が必要。保護者の `parentPlanDecide` と先生の `planApproveTeacher` は画面の `expectedRevision` 必須で、古い画面からの承認を拒否する。
- LINE・電話などの承諾は、先生が実際の `consentDate` と `via` を入力する。授業後・過去月の承諾は `memo` に経緯も必要。実際の記録日時は別に残す。既存データを自動承認したり、承諾日を推測して補わない。
- `billingPreview` は対象月の承認・実績・金額・請求可否を返す。未実施の確定授業、不正な日時・分数、未承認科目、回数超過がある月は請求不可。月謝制は合意した固定額、時間制は実施分数×30分単価÷30を円単位に丸める。承認済み月謝は実施0回でも対象になる。
- `kanriAddPayment` は `studentId,ym,requestId`。金額はサーバーで計算し、請求ID・承認版・料金・実施明細を1行に固定する。同月の有効な請求は1件。同じ要求の再送は同じ請求を返す。旧9列の請求は値を保持してIDだけ補完し、二重発行を防ぐ。
- 請求後はその月の計画・予約・実施状態を変更できない。未入金なら `kanriVoidInvoice` に請求IDと理由を渡して取消を記録し、新しい要求IDで再発行できる。取消済みも履歴に残す。
- `kanriSetPaid` は行番号ではなく `studentId,invoiceId,date,method,expectedPaymentRevision` を使う。画面の入金版が古い変更は拒否し、訂正の再送で新しい入金を消さない。誤入金登録を戻す場合は `unpaid:true,reason` が必要。入金済み請求の取消と入金管理行の直接削除は拒否する。実際の送金・返金を行う機能ではない。

### 5-2. 授業形式と一括確定

人数の判断、予約の保存・再送は [Scheduling.gs](../gas/Scheduling.gs) に集約し、`Code.gs` から呼び出す。

- 形式は `in_person`（対面）/ `online`（オンライン）。生徒の普段の形式は新規案内の初期値で、変更しても既存の予定・過去の授業には連動しない。案内時の指定でその回だけ変更できる。過去の形式が不明な記録は空のまま保持し、実名や当時の形式を公開資料で推測補完しない。
- 時間区間のどの瞬間も対面は2人まで。オンラインは対面を含む他の授業と重ねない。同じ生徒の授業も重複不可。終了と次の開始が同時刻なら重複しない。`offered` と `booked` は席を確保し、取消依頼中も確保を続ける。
- 案内、単件・一括確定、返事のない案内の実施処理、未実施から実施済みへの変更、その回の形式変更に共通判定を適用する。候補または重なる授業の形式が不明な場合は拒否し、先生が確認する。同じ日の有効な授業に不正な時刻がある場合も確認を要する。
- `offer` の `deliveryMode` 省略時は生徒の普段の形式を使う。毎週の案内は最大12回を全日事前確認し、不可の日があれば1件も追加せず理由を返す。生徒のNG日・先生の休みへの `force` 指定で、人数・重複・請求凍結を迂回することはできない。
- `setDeliveryMode` は `studentId,deliveryMode`。`setSlotDeliveryMode` は `studentId,slotId,deliveryMode,expectedMode,requestId` を受け、画面が見た形式との一致を確認する。実施済み・請求確定月は変更不可。確定授業のCalendar・Meetも形式に追従させ、外部処理を確認できなければ元の形式を保って再試行を促す。
- `acceptMany` は `k,slotIds,requestId,expectedSnapshots`（単件 `accept` は `expectedSnapshot`）。1〜31件の重複しない案内を受け付け、本人・時間帯・対象月・科目別の承認回数を選択全体で検証する。案内日時の確定は保護者の月間料金承認とは別の操作で、後者を代替しない。通常の `accept` も同じ保存・再送処理を使う。
- `editOffered` は `studentId,slotId,requestId,expectedSnapshot,date,start,min,subject,deliveryMode`。案内中の枠だけを変更し、元と先の月の請求凍結、定員、休みを検証する。休みへの例外確認は画面内で行う。`offerEdits` と先生APIの `pendingEdits` から同じ内容で復旧し、復旧先の席も保護する。生徒の確定時は表示した日時・科目・形式との一致を検証し、古い画面なら再取得と再確認を要する。
- 保存途中では `pending`、完了件数、授業別の結果を返す。画面は同じ `requestId` と選択内容を保持して再送し、最後の状態取得をまとめる。再読込や別端末でも、本人の状態APIに返す `pendingAccepts` から元の処理を復元する。全件の予約が保存済みで最後の処理記録だけが未完了の場合も復旧できる。処理中の授業の削除・予約解除・形式変更などは復旧まで保留する。事前検証で不可の選択があれば予約と処理記録を新規保存しない。
- Calendarは処理IDと授業IDから決めたイベントIDで登録し、応答が失われても既存イベントを再利用する。連携が有効なオンライン授業はMeet生成中も確定・形式保存を保留し、同じ操作の再試行で完成を確認する。対面ではMeetを発行しない。先生向けの確定通知は選択分を1通にまとめる。送信結果が不明なら予約完了と区別して警告し、重複を避けるため自動再送しない。新規Calendarイベントには生徒を招待せず、更新・取消も `sendUpdates:none`。生徒向け通知は受信確認済みメールの経路に統一する。既存イベントの参加者は一括削除しない。テスト生徒の実メール・Calendar連携は抑止する。

### 5-3. 家族アカウントと保護者通知

授業計画は月単位で基本授業時間 `monthAgreements.lessonMin`（30〜240分・30分刻み、入力既定90分）を保存し、保護者には月・科目・時間×上限回数・1回の料金を表示する。30分単価は実績請求の内部計算に保持する。見送り時だけ科目別の減額ではなく回数縮小と任意伝言を受け付け、`approvedPlanJson` に実際の承認上限を保存する。元の案内回数は `planJson` に保持する。0回は見送り、確定済み回数未満への縮小は先に先生へ取消・見直しを相談する。提案を変更すると旧承認は失効し、再承認が必要。授業時間と承認上限は監査スナップショットにも残す。


保護者ヘッダーのベルから、所属する全生徒の通知を重要度順に確認する。料金提案・未入金は要対応、授業案内・変更・取消と取消申請への回答、先生の返信・公開授業報告を表示する。通知の既読は `familyNoticeReads`（家族ID・通知ID・日時）へ保存し、承認・入金・授業報告本文の既読とは区別する。要対応は通知を読んでも残り、実際の承認・入金を取得した時点で消える。初回データ取得後と明示的な更新時に一覧を取得する。ログアウトは保護者の設定ページに配置する。

保護者ホーム・予定ページの先頭に、子どもの今後の授業予定カレンダーを表示する。日付から授業の詳細を確認でき、先生の休み・空き時間は表示しない。保護者ページは所属する子ども全員を初期表示する。複数人の場合だけ「全員／各生徒」で絞り込み、1人の場合は選択欄を出さない。各生徒の承認・連絡・取消申請・既読はその生徒IDで操作し、アカウント設定と家族の請求書は共通で1か所に表示する。

今回の実装・検証対象。業務上の区分は本節、認証・メール確認・復旧・APIの詳細は [PARENT_AUTH.md](PARENT_AUTH.md)、処理の正本は [FamilyPortal.gs](../gas/FamilyPortal.gs)。

- 兄弟姉妹は別々の生徒として登録し、予定・授業記録・宿題・月間承認・請求を子ども別に保つ。先生が家族アカウントに子どもを明示的に紐付け、招待する。同姓・同一メール・既存の生徒別保護者ログインだけでは家族の閲覧権限を付与しない。
- 保護者が登録し、確認メールで受信を確認したアドレスを家族のログインID・通知先に使う。`students.email` の生徒宛て連絡先とは独立する。メール未確認のアカウントには子どもの情報を返さない。旧 `parents` は維持し、家族への自動統合は行わない。
- 家族画面は紐付いた子どもを切り替えて予定・料金承認・請求を確認する。各読取・承認で対象の子どもへの権限を確認し、先生の内部メモ、未公開下書き、専用リンクの鍵を返さない。子どもの停止・紐付け変更・アカウント停止では既存セッションの権限を失効する。
- 保護者への業務メールは月間回数・料金の提案、請求記録、請求取消が対象。メールにはログイン案内を置き、詳細は保護者画面で確認する。同じ業務イベントの再送は `familyOutbox` の記録を使い、先生が送信状態を確認できる。未送信と確認できたものだけ再送し、結果不明のメールは自動再送しない。送信障害で料金承認・請求の保存を取り消さない。
- 家族を登録しただけで既存の授業や料金を承認しない。実際の兄弟の追加登録、先生からの招待、保護者本人の実メール登録・確認が本運用に必要。家族合算の請求発行は今回に含めず、[合意済み・未実装の課題](FUTURE_WORK.md#家族合算の請求) とする。

### 5-4. 生徒メールと学習記録の共有

- 生徒専用リンクから本人がメールを登録する。`studentEmailRequest` / `Resend` / `Remove` は `k`、`Verify` は独立した30分・1回限りの確認情報を使う。確認メールのURLは `/yoyaku/#student-email?verify=...`。確認リンクは閲覧権限を付与せず、確認情報の原文は台帳へ保存しない。
- 既存 `students.email` は維持するが、受信確認するまで業務メールを送らない。確認後の案内・変更・取消（取消依頼を却下した回答を含む）を通知する。確認中のメール変更は旧確認済み宛先を維持し、先生がアドレスを変更すると確認が失効する。専用リンク再発行・停止で未使用の確認リンクが失効する。
- 生徒は「メール通知」で項目別（案内・変更・取消・取消依頼への回答）にオン・オフを選べる（`studentEmailPrefs`、本人の `k` が必要、`state.emailStatus.prefs` で返す）。行がなければ全項目オン。オフの項目は `studentEmailOutbox` に `skipped` と理由を残し、先生の送信状況から再送できない。受信確認メールには適用しない。先生の生徒設定にはオフの項目を表示する。保存先は `studentEmailPrefs` シート（studentId, offered, changed, cancelled, cancelDeclined, updatedAt。'1'/'0'）で、初回保存時に自動作成する。
- 同じ通知は `studentEmailOutbox` で重複送信を防ぐ。送信失敗と結果不明を区別し、未送信と確認できるものだけ先生の送信状況から再送する。授業取消の保存と通知予約の間で終了した場合は `slotChangeNotices` から先生の次回読取時に通知を回復する。メール本文に生徒専用リンク・先生メモ・記録本文は載せない。登録・通知解除は家族のメールに影響しない。
- 今後の授業記録保存は生徒本人・紐付いた保護者へ自動公開する。先生だけのメモと報告下書きは非公開。既存の記録を一括公開せず、既存記録も次の保存時に公開する。無効化で記録の公開を取り消す。保存完了前は直前の公開版を保つ。
- 宿題期限は日付指定・次の同じ科目の確定授業・期限なし。次回授業がなければ予定未定、予定変更・取消には連動し、完了時点の期限を固定する。授業記録の宿題は記録保存で公開されるが、「やること」への追加・更新は先生の反映操作で行う。詳細と理由は [授業サイクル仕様](LESSON_CYCLE_PHASE1_SPEC.md) が正本。

## 6. 更新・デプロイ手順

関連する変更を、利用の流れを通して検証できるまとまりにして公開する。開発中は作業ブランチで実装・レビュー・ローカル検証を進め、復元用のコミットは小さく残す。途中の機能ごとに本番へ反映せず、まとまりが整ってから必要な退避・実環境確認・公開をまとめて行う。未完成の変更をPages配信元のmainへpushしない。

緊急の不具合修正や、単独で移行・復旧を確認すべき認証/台帳変更は、公開を分ける。Mail・Calendar・権限・Sheetsの日付型などローカルで再現しきれない不確実性は、必要最小限の隔離した実GAS検証で早めに確認する。まとめることを理由に必要な検証や復旧手段を省かない。

1. Gitの差分と対象の本番版を確認し、変更範囲に応じて検証する。JS/GASは `npm run check` と影響するテストを実行し、公開するまとまり全体の影響が広い場合は `npm test` を使う。認証変更の対象テスト・実GAS確認は [認証の検証条件](PARENT_AUTH.md#今後の再反映時の順序と確認) に従う。開発中は変更した部分の確認を中心とし、全テストを小修正ごとに繰り返さない。文書だけなら参照の整合確認でよい。
2. GASを変更する場合は、公開前の実稼働ソースを退避してGitの本番基準と比較する。対象の本番版に対応する退避と比較記録があり、その後の変更がないと確認できれば再利用する。データ移行・構造変更では対象台帳もコピーし、内容・構造を比較してから進める。同じ公開作業の途中で理由なく退避を作り直さず、対象が更新された場合や復旧範囲が広がる場合は取り直す。`ensureSchema_` / `LEDGER_COLS` の自動追加任せにせず、既存列との互換性と準備手順を確認する。初期設定用 `setup()` を移行のために再実行しない。
3. Apps Scriptエディタに変更した `gas/*.gs` だけを反映・保存し、読み戻した全文を改行正規化後に照合する。本体は `Code.gs`・`LessonCycle.gs`・`BillingApproval.gs`・`Scheduling.gs`・`FamilyPortal.gs`・`StudentEmail.gs`・`LearningServices.gs`・`Backup.gs` の8ファイル。変更していないファイルは、本番基準との差分や他の編集が疑われる場合に照合する。「保存しています」等の表示だけで判断しない。構造変更時は既存公開版を維持し、互換性を確認した準備処理で列を追加・確認してから公開を切り替える（v48までの準備例は `prepareStepwise20260908`）。既存値の補完は対象と差分を確認して別に行い、不明値は推測で埋めない。一時検証ファイル（`test/gas-native-*.gs` など）は削除し、既存の「デプロイを管理」→鉛筆→「新バージョン」で公開する（既存URLを維持）。Codexでもブラウザ経由の反映を実施済み。通常の反映は [claspの自動反映手順](#backup-release-automation) を使い、このブラウザ手順は代替手段とする。
4. mainへ `git commit` → `git push origin main` でGitHub/Pagesを更新する。**新APIに依存する画面はGAS→HTMLの順**にし、旧画面との互換性も確認する。独立した画面・文書変更ではGASの再デプロイは不要。
5. Pagesのビルド完了と対象ファイルの公開内容を確認する。GAS更新時は `/exec` の `release` が公開対象のソースと一致することと、変更機能のテスト生徒による疎通を確認する。公開後は変更した利用の流れをまとめて確認し、修正・失敗・未解決の懸念がない限り、通過済みの検証を繰り返さない。通知・カレンダーを動かす検証は対象と設定を確認し、作成したテストデータを片付ける。

画面は対象コミットのrevert→pushで戻す。GASは既存デプロイの版を戻せるが、スキーマや認証の互換性を確認して選ぶ。コードの差し戻しと台帳の復旧は分け、無関係の授業・入金まで巻き戻さない。保護者認証の旧版復帰の制約は [戻し方](PARENT_AUTH.md#戻し方)。

### 6-1. v46の反映と検証

- 2026-09-08 01:36 JST、既存のWebアプリURLを維持してGAS v46を公開。`/exec` のhealth応答で `release: 2026-09-08-lessons-billing` を確認。対応コードはmainの `f861fbb` へpush済み。
- 実GASでは新規の架空台帳だけを使い、認証32項目・授業サイクル20項目・月間承認と請求41項目、計93項目がすべて通過。授業の数式評価防止、途中保存からの再開、宿題の完了保持、請求の再送と履歴保護、通知・Calendarへの副作用がないことを確認した。ローカルのAPI/UI回帰100件と、本番の架空保護者の認証・同時ログインの確認も実施済み。
- 移行前に2台帳をDrive「ステップワイズ塾/授業記録・承認請求反映前バックアップ_20260908」へコピーし、全18シートの構成と対象見出しを照合。元データ比較はparentsを除く17対象シートで、比較対象範囲の差分は0。configはキー・ヘッダーのみを比較した。編集前のGASソースはGit基準 `9a7b16e` と一致した。
- 移行時の本番の追加6シート（lessonRecords / lessonPrivateNotes / lessonReportDrafts / lessonWrites / monthAgreements / approvalEvents）はヘッダーのみで、過去データは自動生成していない。tasksは12列、入金管理は21列へ移行済み。移行後も既存17シートの旧列の入力値・実効値は比較対象範囲で差分0、新規6シートの見出し・空データ、旧シートID・寸法・相対順序を照合した（parents除外、configキー・見出しのみ）。
- 詳細結果は非公開の `.verification/implementation-20260908/native-all-result.json` と `migration-result.json` に保存。バックアップ・架空台帳のIDや認証情報は公開文書へ転記しない。
- Pagesビルドrun `34144055886` が成功。公開 `/kanri/` と `/yoyaku/` を `?nc=` 付きで取得し、`f861fbb` のHTMLと改行正規化後の全文一致を確認した。
- Chrome本番画面ではテスト生徒を使い、保護者ログイン・セッション復帰、先生の当月1回の計画保存と1,500円の提示、授業内容・先生メモ・次回の焦点・宿題の保存→宿題反映→生徒の完了操作、未公開の報告下書きの生成・保存、授業の実施記録を確認した。請求プレビューは1回30分・1,500円となり、請求記録の保存まで成功した。
- 公開の保護者APIでも承認済みの月間計画と1,500円の未入金請求1件を確認し、生徒・保護者への返却に先生メモと未公開下書きが含まれないことを確認した。
- **確認方法の制約**: Chromeの標準確認ダイアログで操作ツールのフォーカス処理が進まなくなったため、保護者の月間承認と生徒の予約確定（accept）は同じ公開APIで補完した。この2操作は画面ボタンから最後まで確認したとは扱わない。上記の先生向け新UIの操作は画面で確認済み。
- **後片付け済み**: 2026-09-08 02:26:11 JST、対象テスト生徒の未入金請求を理由付きで取消し、宿題取り下げ（完了状態保持）、記録無効化、予約解除・枠削除、当月計画の0回への変更を完了。40項目の最終チェックがすべてtrueとなり、新規予定・有効請求・公開宿題・active記録・有効承認・未完了journalがいずれも0であることを確認した。取消請求・無効記録・内部メモ・下書き・監査履歴は保持する。
- 後片付け後の公開APIでも、対象生徒のslots/tasksが0件、保護者側の有効請求・有効承認が0件、取消請求1,500円が履歴として1件残り、内部情報が返らないことを確認した。テスト保護者セッションはログアウトし、失効後の拒否も確認済み。一時GAS検証ファイルは除去し、本体3ファイルのみ保存した。この後片付け時点の公開版はv46。非公開の結果は同ディレクトリの `production-cleanup-result.json` と `production-flow-check.json` に保存した。
- 月間計画の途中保存・再送への追加修正は、影響対象のローカルAPI46件・UI17件と構文確認が通過。2026-09-08 02:43:01〜02:44:19 JST、新規の架空台帳2冊で実GAS49項目がすべて通過し、保持済みの料金・版・監査1件を保った復旧、古い版の拒否、過去月の最後の計画行を削除した後の復旧、通知・Calendarへの副作用がないことを確認した。非公開の結果は同ディレクトリの `native-billing-r2-result.json` に保存した。
- 2026-09-08 02:46 JST、既存のWebアプリURL・デプロイIDを維持して追加修正をGAS v47へ公開。`/exec` のhealth応答で `ok:true` と `release: 2026-09-08-lessons-billing-r2` を確認した。本体3ファイルの全文をローカルと照合し、一時検証ファイルを除去した状態で公開した。追加の台帳移行は不要。公開後の生徒APIでも対象テスト生徒のslots/tasksは0件で、再試験による本番の追加データはない。

### 6-2. v48の反映と検証

- 2026-09-08 04:15 JST、既存URL・アクセス範囲を維持してGAS v48へ公開。`/exec` の `release: 2026-09-08-operations-family` を確認した。本体5ファイルを読み戻してローカルと照合し、一時検証コードを除いて公開した。Pagesは `89d5b51` のビルド `34154952511` が成功し、公開の管理・生徒HTMLの全文一致を確認した。
- 移行前の2台帳をDrive「兄弟・授業定員・一括確定・保護者メール反映前_20260908」に退避し、23対象の比較を実施した（parentsは除外、configはキーのみ）。生徒12行の普段の形式と今後の授業9行の形式を補完し、studentsの既存9列・slotsの既存11列が変わらないことを内部比較した。不明な過去値や既存Calendar・Meetは維持した。家族は空の状態から開始し、実保護者を自動作成していない。
- ローカル回帰231件と構文確認が成功。新規の隔離台帳で家族認証45項目・授業形式と一括確定44項目が通過し、認証の失効・子別権限・通知の再送抑止・人数上限・途中確定からの復旧を確認した。実Calendarでは招待先のない架空予定1件で生成・同一イベント再利用・対面/オンライン切替・Meet生成/除去の9項目を確認し、予定を削除した。
- 本番Chromeでは、先生本人が指定した宛先への確認メールを1通だけ送信し、Gmail受信→確認リンク→メール確認→保護者ログイン→架空の子ども2人の切替→12回の月間計画承認→生徒側の全件選択・日時確認・12回一括確定を画面から実行した。保護者画面の12件の確定表示、先生の家族・通知画面のメール確認済み/送信済み、保護者ログアウトも確認した。実保護者へのメール・招待は送っていない。
- 04:26 JST、後片付け61項目がすべて通過。新しいテスト生徒の12枠と月間計画を除去し、有効承認を解除、生徒を停止、専用リンク・家族のメール登録・パスワード等を失効/消去した。元のテスト生徒と他の生徒・予定が変わらず、通知・処理・承認の監査履歴が保持されていることを比較した。公開画面でテスト用リンクの失効、公開APIで旧テスト生徒の予定/宿題0件と家族データへの未認証アクセス拒否を確認。一時GASファイルを除去し、本体5ファイルのみ保存した。検証用メールの追加送信はない。
- 予定表の連鎖した重なりは、同時人数と区別して「時間が重なる授業」と表示する。最大同時2人の4枠が「4人同時」に見えないことを局所UIテストで確認した。
- 詳細な照合・検証結果は非公開の `.verification/operations-20260908/` に保存。バックアップ・架空台帳のID、認証情報、実生徒の情報は公開資料に転記しない。追加した台帳・認証は破壊せず、復旧時は版の互換性を確認する。

### 6-3. v49の反映と検証

**公開状況: GAS v49・Pagesは反映済み。先生・本人画面の対象操作、テストデータの後片付けと専用リンクの失効を確認済み。**

- 2026-09-08 10:37 JST、既存URL・アクセス範囲を維持してGAS v49へ公開した。healthの `ok: true` と `release: 2026-09-08-learning-updates` を確認した。本体6ファイルを読み戻してローカルと全文照合し、一時検証・移行・訂正ヘルパーを除去して公開した。
- Pagesは `1445afc` のビルド `34177409630` が成功し、公開の管理HTML 208,202文字・生徒HTML 131,547文字がローカルと全文一致した。
- 公開画面は専用の `【テスト】` 生徒で確認した。案内の日時・科目編集は同じ枠IDと全3枠を保持し、保存した授業記録の本文・宿題が本人画面と公開APIへ1件現れ、先生メモはどちらにも含まれなかった。宿題は次回の同じ科目の授業日時を期限に反映され、本人の完了操作後も期限を保持した。先生ホームの授業・休みの時刻順、化学の選択、本人メール画面の案内・変更・取消の説明も確認した。追加の実メール送信は行っていない。保護者の表示範囲は今回の実GAS検証で確認し、新しいテスト保護者での実画面ログインは省略した。
- 追加対象は案内の日時・科目・形式の編集、ホーム予定表の時刻順表示、化学の選択、生徒メールの受信確認と通知、授業記録の保存時公開、次回の同じ科目の授業を期限にする宿題。仕様は4章・5-2節・5-4節と [授業サイクル仕様](LESSON_CYCLE_PHASE1_SPEC.md) を参照する。
- ローカル検証は全体352件の通過後、追加した3件も通過し、重複を除く355件を確認した。実GASでは隔離した架空台帳を使い、授業案内・形式・一括確定53項目、授業記録29項目、生徒メール36項目の計118項目がすべて通過した。
- 移行前の2台帳をDriveの「99_アーカイブ」に退避し、両帳の全29シートについて値と数式の一致を内部比較した。設定・認証情報も照合対象としたが、内容や比較用ハッシュを公開結果に含めていない。
- 本番の予約台帳は元の24シートを保持し、`lessonPublicSnapshots`、`slotChangeNotices`、`offerEdits`、`studentEmails`、`studentEmailOutbox` の5シートを空の状態で追加した。tasksは既存12列を保って16列へ拡張した。移行時に予約台帳の既存2,017セルと管理台帳の259セルの値・数式保持を確認した。
- シートの追加位置に依存せず、既存シートを名前・IDで照合したうえで、追加5枚を末尾へ移した。既存シートの元の順序と選択中のシートを復元し、並べ替えによるセル変更がないことも確認した。確認関数は、既存値・数式・IDの差や追加シートへの業務行の混入があれば停止する。
- 構造準備とは別に、ユーザーが科目を確認した既存授業1件について、バックアップ・現在の所有者と日時・請求・記録との整合を確認して、空欄の科目セル1つだけを「化学」へ訂正した。他の全slotsセルの保持を確認し、承認・料金・実施状態・通知・Calendarは変更していない。対象の個人名・IDは公開資料に記載しない。
- 月間回数・実際の料金・承諾の未確認事項は今回の科目訂正では解消しない。既存の承認や料金を推測で補わず、[月間回数の本運用](FUTURE_WORK.md#月の授業回数方式aの本運用開始) の残件として維持する。
- 10:49 JSTに専用テスト生徒を停止し、所有する授業枠・有効な記録と宿題・公開記録・確認済みメールと確認情報が残っていないことを確認した。履歴を保持し、他の予約・管理台帳の業務行が変わっていないことも照合した。元の専用リンクは公開APIで本人なし・枠/宿題/公開記録0件となり、本人画面も専用リンクから開く案内へ戻った。後片付け用の一時関数を除去し、本体6ファイルのみでローカルと全文一致することを再確認した。証跡と一時関数は非公開の `.verification/learning-20260908/` に保存する。

### 6-4. v50の反映と検証

- 2026-09-08 20:38 JST、既存URL・デプロイID・アクセス範囲を維持してGAS v50へ更新。healthの `release` は `2026-09-08-teacher-workflow`。Pagesは `4af154f` を公開し、両HTMLの全文一致を確認。2人表示の氏名が固定メニューに隠れない補正を `bb5bf3b` へ追加した。
- 稼働中の6ファイルを退避し、v49のGit基準とすべて一致。変更した4ファイルは保存・再読み込み後にもローカルと全文照合した。一時検証ファイルを除いた状態で公開した。
- 2台帳をDriveへコピーし、予約29シート・管理5シートの順序、ID、寸法、全既存値・数式・設定を比較した。予約台帳末尾に空の `lessonPreparations`（10列）、既存 `wishes` の末尾に3列を追加。旧セルと管理台帳の保持、新規データ欄が空であることを確認した。
- ローカルの既存385検証に加え、公開時に変更対象30検証と構文チェックを実施。隔離した実Google Sheetsで41項目が通過。準備の非公開性・文字列保存・再送、2人の取得、満員表示後の希望保存・再送、既存の記録公開・宿題期限・中断復旧を確認した。Mail・Calendarの呼び出しは0件。
- 本番の専用テスト生徒2人で、ホーム・個別ページの過去の未実施登録表示、生徒設定の分離、未来の準備保存、2人の独立した記録保存、片方の保存中の他方の入力保持を確認。本人画面で満員・要調整を確認して希望を送り、送信時の状態が残ることも確認した。両本人APIで自分の記録だけが公開され、先生メモ・準備本文・他方の記録が含まれないことを照合した。保護者の新規ログインと実メール送信は追加実施していない。

- 20:50 JSTにテスト生徒2人を停止・専用リンクを失効し、テスト授業5件と希望を削除、記録2件を無効化した。記録履歴を保持し、他の予約・管理台帳の業務行が変わっていないことを照合。一時検証ファイルは削除し、本体6ファイルに戻した。
- テスト生徒での単発計測は概要3,730ms / 2,356bytes、設定1,054ms / 533bytes、全体5,547ms / 3,693bytes。同一実行内で順に計測したサーバー処理の参考値で、通信・起動待ちを含む体感速度や旧版との比較ではない。証跡は非公開の `.verification/teacher-workflow-release-20260908/` に保存する。

### 6-5. v51・テストデータと旧空き枠の整理

- 2026-09-08 22:16 JSTに既存URLのGASをv51へ更新。`release: 2026-09-08-cleanup`、コードは `67962bf`。授業解除で未割当の `open` 枠を残さず削除する。取消の請求制約・Calendar削除・通知・中断再送は従来の処理を維持する。
- 2台帳をDriveの既存アーカイブへ退避し、全シートの値・数式が一致してから整理した。確認済みのテスト生徒13件・テスト家族1件・関連する記録/認証/請求/ログ、旧空き枠6件を除去。通常操作ログの除去はテスト関連と旧枠追加の120件。実生徒を含むログは残す。
- 22:14 JSTの整理後に予約30シート・管理5シートを期待差分と照合し、全35シートが一致。実生徒4人・授業17件、実生徒の成績・記録・家族・設定・授業不可時間を保持した。「一覧」は派生値の変化を許容し、見出し・数式を照合した。復元用の退避・対象一覧・証跡は非公開の `.verification/cleanup-20260908/` に保存する。
- 構文確認と取消・通知・請求・定員の97検証が通過。変更したCode.gsは本番v50から退避しGit基準と一致、変更後も保存・再読み込み後に全文照合。一時整理関数を除いた本体6ファイルで公開した。テスト生徒の停止・履歴保持だけだった過去の検証記録は当時の結果として残し、今回その履歴を退避先へ移した。

### 6-6. v52・MCP 登録ツール

- 2026-09-08 22:51 JST、既存URL・デプロイIDを維持して GAS v52 へ更新。`release: 2026-09-08-mcp-writes`。変更は `Code.gs`(MCP 更新系 op と書き込み範囲)と `Scheduling.gs`(先生の代理登録では先生宛ての希望メールを送らない `req.proxy`)の2ファイル。公開前にエディタ6ファイルのハッシュが Git HEAD(`dc56fae`)と一致することを確認し、置換適用後・保存・再読み込み後にローカルと全文ハッシュ一致を確認した。他4ファイルは変更なし。
- ローカル検証: `test/mcp-writes.test.cjs` 11件を追加し全体 396件が通過(`npm run check` も通過)。本番は health の release、`mcpPing` の writeOps / writeScope、範囲制限(`scope`)と不在生徒の拒否を確認。実生徒への登録は行っていない。MCP サーバー側は `stepwise-mcp` 0.2.0(Worker Version ec4f2bf5)。

<a id="services-release-v53"></a>
### 6-7. v53・取消申請／成績票PDF／連絡欄

- 2026-09-09 01:18 JST、既存URL・デプロイID・実行者・アクセス範囲を維持してGAS v53へ更新。`release: 2026-09-09-learning-services`。既存6ファイルを退避してmain基準と全文照合後、`Code.gs` と追加の `LearningServices.gs` を保存・再読み込み・全文照合した。一時検証ファイルは削除して公開した。
- 2台帳をDrive「成績票・連絡・取消反映前バックアップ_20260909」にコピーし、35シートの構成・値・数式を照合。新規3シートを追加後も、元の台帳の既存セルが保持されていることを確認した。復旧コピーのIDはScript Properties `SERVICES_RELEASE_BACKUP_20260909` に保持している。初期化の `setup()` は実行していない。
- 追加権限は先生本人が承認した。新規の隔離テスト台帳・非公開フォルダで15項目（実DriveのPDF保存/取得、認証、所有者以外の拒否、先生メモ非公開、再送、返信、取消受付と予約保持）を確認し、検証用台帳・PDF・フォルダはゴミ箱へ移動した。実生徒のデータへ検証の書き込みはしていない。
- 構文検査と410件の自動テストに合格。Pagesは `4817e3a` のビルド成功と管理HTML・予約HTML・共通JSの全文一致を確認した。匿名HTTPアクセスで新リリースと不正な専用リンクの拒否、ログイン済み管理画面で受信箱と生徒個別の成績票・連絡パネルの取得を確認した。
- 公開直後、Googleの応答先へのアクセスでタイムアウトと一時的な404が発生したが、再確認で匿名HTTPとブラウザの両方から正常応答を得た。定常的な応答速度の改善を確認したものではない。
- MCPとAIの定期実行は追加していない。実装範囲・保存期間等の残件は [FUTURE_WORK.md](FUTURE_WORK.md)。退避・照合・検証記録は非公開の `.verification/services-release-20260909/` に保存する。

### 6-8. v54・連絡欄の MCP 処理

- 2026-09-09 13:16 JST、既存URL・デプロイIDを維持して GAS v54 へ更新。`release: 2026-09-09-mcp-inbox`。変更は `Code.gs` のみ(MCP 連絡欄 op と処理ジャーナル、登録 op の紐づけ検証)。公開前にエディタ7ファイルのハッシュが Git HEAD(`3d67852`)と一致することを確認し、置換適用後・保存・再読み込み後にローカルと全文ハッシュ一致を確認した。新規シート `contactProcessing` は初回の claim 時に作成される(既存シートの変更なし)。
- ローカル検証: `test/mcp-inbox.test.cjs` 6件を追加し全体 416件が通過(`npm run check` 通過)。本番は health の release、`mcpPing` の readOps / writeOps、`mcpInboxList` の応答、存在しないメッセージへの claim / resolve / 紐づけ登録の拒否を確認。実在の連絡は処理していない。MCP サーバー側は `stepwise-mcp` 0.3.0。

## 7. 運用メモ

<a id="calendar-parent-ui"></a>
### 週間予定表と保護者専用の画面（2026-09-09）

- 管理側の授業ページは週間予定表を先頭に表示。通常は7:00〜22:00を表示し、時間外の授業がある週は件数と表示切替を案内する。空き時間のクリック・タップ、マウスの縦ドラッグから日時・長さを入れた案内ダイアログを開く。ドラッグは30分単位・最大120分、フォームでは既存の30/45/60/90/120分と毎週の繰返しを利用する。操作の参考は [Google Calendarの予定作成](https://support.google.com/calendar/answer/72143?hl=ja)。
- 7:00より前の開始または22:00を超える終了は、案内時に「範囲外を指定する」が必要。これは通常の画面操作の既定値であり、過去データの変更やGAS/MCPへの営業時間制限ではない。既存の時間外授業は保持する。
- 先生の授業不可と授業を同じ時間軸に表示し、生徒の授業不可は対象生徒を選んで重ねる。帯を選ぶと詳細・解除操作を表示する。全期間の授業不可一覧は廃止し、代理登録と月間予定表は必要時に開く。フォームの重なり表示は補助で、送信時の既存GASによる定員・休み・料金条件の検査を維持する。
- `/hogosha/` は家族認証用の独立した入口。「ホーム・予定・授業報告/宿題・成績・請求/料金承認・連絡・設定」に分け、認証済みの子ども切替を共通に置く。既読は報告を開いたときだけ、承認は料金ページの既存確認操作で保存する。旧 `/yoyaku/#family`、確認メールのリンク、生徒別の旧保護者認証は互換を維持する。
- 生徒と保護者の見た目・処理の正本は `assets/portal.css` / `assets/portal.js`。各HTMLは入口で、ロジックを複製しない。API URLは管理HTMLと `assets/portal.js` を参照。管理画面の生徒プレビューボタンを削除し、旧 `?preview=` は生徒キーの切替に使わない。
- 画面だけの変更でGASはv56のまま。全442件のテスト後、最終調整の対象テストも確認。隔離ブラウザで時間選択→案内の登録・予定表への反映、保護者のタブ分割・未読から既読への切替・連絡欄を確認した。実生徒への検証用の案内・請求・メール送信は行わない。

<a id="local-lesson-workspace"></a>
### 授業情報の集約・実施分請求・家族明細

2026-09-09、第一段階をGAS v56へ反映。公開healthは `2026-09-09-lesson-workspace`。以下は現行仕様である。

- 既存 `lessonRecords` / `lessonPublicSnapshots` の末尾に `reportJson` を1列追加する。担当講師、予定/実施単元、理解度、進度、前回宿題の取り組み・正答率、同科目のテスト日、保護者への連絡を保存し、既存の記録版・処理ジャーナル・公開確定条件を共用する。別の報告書台帳は作らない。従来のクライアントが項目を送らない場合、保存済みの追加情報は保持する。
- 追加情報は先生だけが編集でき、保存確定後に本人・紐付いた保護者へ公開。非公開メモ・準備・報告下書きは混ぜない。正答率は空欄または0〜100%（小数1桁まで）。単元・理解度等を推測補完せず、本文は3行程度の入力欄でも切り捨てない。
- 月内順序は同じ生徒・科目の現在の確定授業を日時順に数える。月間計画の回数は別に表示し、案内中は確定数に含めない。次回は同じ科目の次の確定授業、日数は表示中の授業日からの日数。テストまでの回数は今回を除く、指定したテスト日までの同科目の確定授業数。予定変更に追従する値であり、授業当時の確定回数の記録ではない。
- 前回の同科目の宿題を完了分も含めて表示する。生徒向け公開記録の宿題は既存tasksとの対応・完了状態を表示し、反映済みかつ取り下げられていない課題だけ本人がその場で完了操作できる。記録保存とtasksへの反映を勝手に同時化せず、既存の再送・完了保持の条件を継承する。保護者の画面は完了状況の閲覧。
- 新規の料金提案・設定は30分単価で、固定月謝の非ゼロ入力を拒否する。過去月は単価の明示が必要。旧固定月謝の提案/承認は新規の承認・請求を停止して再提案を促す。過去の確定請求は保存額を維持し、新規の実施0回の請求は作らない。旧studentsの月謝列は証跡として残し、単価編集で黙って消さない。
- 新規請求の授業明細は実施時間から金額を確定する。端数は日時順の累積時間の丸め差分で配分し、明細合計を請求額へ一致させる。過去の明細に金額がなければ推測補完しない。
- 家族・通知画面と保護者の家族ページに、既存の子ども別請求ID・請求日・実施明細・小計・家族合計・未入金額を表示。取消請求は除外。同じ子ども/月に重複請求があれば合計を出さず確認を促す。現在の認証済み家族の子どもだけが対象で、閲覧で請求発行や入金の書き込みをしない。家族構成を固定した正式な請求書、一部入金の配分、PDF・メール発行は次段階。

検証: 既読追加後の全436件・構文確認は通過。家族明細の認証境界、旧請求額の保持、架空データによる共通画面のブラウザ表示を確認した。

**保護者の既読（v56）**: 確認印・確認ボタンは作らず、保護者の授業記録一覧に「未読／既読／更新あり」を表示する。記録の折りたたみを開いた時だけ `learningService` の `recordRead` を呼び、一覧取得は `recordReadStatus` の読取のみ。生徒画面は既読を書き込まない。

保存先は `lessonReadReceipts`（id/studentId/readerId/recordId/revision/readAt）。認証済み家族アカウントID、または旧保護者認証の生徒別アカウントを使い、閲覧者をクライアントから指定させない。兄弟・別家族・旧認証間で既読を混同しない。同じ記録の再送は保存済みの版・日時を保持し、古い版で新しい公開版を既読にできない。未公開・無効化・他生徒の記録も拒否する。料金承認や授業の実施状態は変更しない。

画面では保存成功後に既読へ切り替える。通信失敗時は未保存と再試行方法を示す。サーバー側で既に新版が公開されていた場合は「更新あり」と再読み込みを案内する。ログアウト・子ども切替後に届いた応答は前の画面へ反映しない。本文を理解・了承した証明ではなく、記録を開いた履歴のみを意味する。先生向けの既読管理画面は追加しない。

公開前のバックアップは15:11 JSTに成功。`ensureLessonSchema_` の末尾列追加と `serviceEnsure_('lessonReadReceipts')` を実行し、2台帳の既存38シートの元範囲の値・数式が保持されたことを照合した。旧固定月謝の生徒設定・月間承認はともに0件で、料金再提案の対象はなかった。

実GASの隔離台帳で、報告保存・追加情報の往復・非公開メモの除外・同科目の次回・未読/既読・再送・学生/他生徒の拒否・旧クライアント編集時の保持・新版の既読管理を確認した。隔離台帳は処理終了時にゴミ箱へ移し、一時検証コードは公開版から除外。実生徒の記録や請求に検証用の書き込みはしていない。v55のGit基準 `40b91f1` から、全8ソース・manifest・固定版を読み戻して照合した。反映記録はGit対象外の `.verification/releases/2026-09-09T06-11-37-342Z/`。

PagesはGAS切替後にmainへ反映し、2画面と3追加アセットの配信内容をローカルと照合。認証済み先生画面で授業報告の追加項目・月間回数・次回表示と家族一覧の正常応答を読取確認した。実保護者での既読保存や実請求の発行は検証目的では行わず、隔離GAS・認証境界のローカルテスト・配信ファイルの一致で確認範囲を分けている。

- 検証は隔離台帳を優先する。本番で専用テスト生徒を使った場合は、復元用の退避と実データの差分照合を行い、検証用の関連データ・家族・操作ログも通常環境から片付ける。停止だけで家族の選択肢や「最近の動き」に残さない。実生徒の記録・監査履歴はこの整理対象にしない。
- 先生の予定は授業の案内・確定と授業不可時間で管理する。旧方式の未割当の空き枠は使わず、新規の授業解除では枠を削除する。取消の元情報・通知の再送制御は `slotChangeNotices` に保持する。
- 先生のログインは1か所のみ有効。別端末でログインすると前のログインは切れます。
- パスワードを忘れたら管理画面の「パスワードを忘れた」→ 登録メール(mugilives@gmail.com)に届く6桁コードで再設定。
- 通知メールはApps ScriptからGmailで送信し、確定授業はGoogleカレンダーにも作成する。案内・確定・取消承認・希望・取消依頼・パスワード再設定などは通知や予定変更を伴う。実行前に対象APIの副作用を確認する。MCPの閲覧も `mcpLog` に記録される。
- LINEで受けた生徒のNG日を代理登録するときは、`blocked` のメモに「LINE連絡(日付)」を残す。
- 台帳移行前は対象の最新コピーと照合が必要。週次バックアップは移行直前の退避の代わりにはしない。自動化の実装・復旧範囲は [下記](#backup-release-automation)、認証固有の退避は [PARENT_AUTH.md](PARENT_AUTH.md#今回の確認範囲)。

<a id="backup-release-automation"></a>
### バックアップ・GAS反映の自動化

**2026-09-09: claspでGAS v55へ反映済み。** v54の実ソースをGit基準 `b7e02df` と照合・退避し、全8ソースとmanifestの読み戻し、固定版の一致、既存URLの公開health `2026-09-09-backup-release` を確認した。GoogleログインとApps Script APIの有効化は完了。追加OAuth同意後、初回バックアップは14:17 JSTに成功、独立コピーへの復元照合は14:19 JSTに完了した。元の業務台帳へ書き戻さず、検証コピーを片付けた。週次トリガーは毎週日曜03〜04時（日本時間）に1件、作業用トリガーは0件であることを画面でも確認した。初回はエディタから分割処理を実行して検証し、日曜の時刻起動そのものは次回実行からとなる。反映記録はGit対象外の `.verification/releases/2026-09-09T05-11-15-242Z/`。

#### GASの反映

Google公式 [clasp](https://github.com/google/clasp) 3.4.1をdevDependencyに固定。初回は先生自身が `npm run gas:login` のGoogle認証を行い、[Apps Script設定](https://script.google.com/home/usersettings)でApps Script APIを有効にする。認証情報はユーザープロファイルの `.clasprc.json` に置き、Git・Drive共有資料に転記しない。新しい権限の同意は本人が行う。

1. 関連変更をまとめて検証・コミットする。コード変更時は `Code.gs` のhealthの `release` も更新する。
2. `npm run gas:plan -- <現在本番に対応するGitコミット>`。初回基準は `b7e02df`。省略時は `origin/main` だが、先に新コードをpushした場合は本番基準の代用にならない。
3. 出力された変更ファイルを確認し、`npm run gas:apply -- <出力されたrelease-id>`。台帳の準備が必要なら、先に `npm run gas:stage -- <同じrelease-id>` でエディタだけを反映・照合し、退避済み台帳の移行と検証後にapplyする。一時検証コードはapply前に除外し、計画済みソースとの一致を確認する。本番HEADと公開版が一致しない、Git基準と違う、ファイル削除が必要、計画後に編集された場合は停止する。新しい権限が必要な変更では、既存manifestを保持する本コマンドだけでは権限追加を行わず、別途同意と準備を済ませる。
4. 反映後は全ソースと固定された版を取得して照合し、既存デプロイIDを指定して版を更新、公開healthのreleaseを確認する。画面変更があれば従来どおりGASの確認後にmainをpushし、対象画面を確認する。

退避ソース・manifest・計画と進捗・healthはGit対象外の `.verification/releases/<release-id>/` に保存する。ブラウザ側の「コード.gs」とローカル `Code.gs` の名前を対応付け、公開manifestをそのまま保持する。想定外のファイルは削除せず停止する。失敗時は同じIDのapplyを再実行できるが、書き込みや版作成の結果が不明なら自動で上書きせず調査する。Google側に原子的な比較更新はないため、反映中の並行編集は避ける。

旧公開版へ戻す場合は `npm run gas:rollback -- <release-id>`。この操作は公開版だけを切り替える。エディタHEAD、Sheets、Drive、通知、Calendarは巻き戻さない。台帳の列変更がある場合は旧版との互換性を先に確認する。既存のブラウザ手順は接続障害時の代替手段として残す。

#### 週次バックアップ

`gas/Backup.gs` を反映後、エディタで `setupStepwiseBackups` を一度実行して必要なGoogle権限に同意する。所有者だけがアクセスできる新規Driveフォルダへ、毎週日曜の日本時間3時台に退避する。初回は `startStepwiseBackup` を実行し、`continueStepwiseBackup` の5分間隔トリガーが台帳1冊またはPDF1件ずつ処理する。完了・失敗時は作業用トリガーを外し、他機能のトリガーは触らない。`stepwiseBackupStatus` で状況を取得できる。途中停止時の `startStepwiseBackup` は同じジョブを再開し、失敗済みなら新しい世代を作る。

- 対象: 予約台帳、授業管理台帳、`EXAM_PDF_FOLDER_ID` 内の原資料、Script Properties。設定ファイルには秘密値を含むため所有者限定で保存し、内容をログや公開資料へ出さない。
- 照合: 台帳のシート順・名前・使用範囲・値・数式、PDFの内容ハッシュ、設定内容。処理中に元データやPDFの集合・更新日時・設定が変われば失敗とし、最後に成功した世代を維持する。表示書式やDrive権限の復元を比較済みとは扱わない。
- ソース: 台帳にバインドされたスクリプトは[コンテナのコピーに含まれる](https://developers.google.com/apps-script/guides/bound)。公開済みの版・manifestの明示的な退避は上記CLIで保持する。外部Calendar全体、送信済みメール、OAuthログイン、トリガーそのもの、Git履歴のバックアップは対象外。
- 初回と復旧手順変更時に `verifyStepwiseBackupRestore` を実行する。最新の成功世代から独立したテスト用コピーを作って台帳内容を照合し、PDFと設定の保存内容も再確認する。実台帳には書き戻さず、作った検証フォルダだけをゴミ箱へ移す。失敗した場合は成功記録を更新しない。
- 本復旧は自動実行しない。manifestで対象を確認し、現状の追加退避、差分確認、復元コピーへの切替を行う。コピー先の台帳IDやPDF参照ID、Script Properties、権限、トリガー、デプロイを点検する。**コピー内のスクリプトをそのまま実行しない**（コードの台帳IDは元環境を指す）。設定はJSONのキー・値ペア配列で保存する。秘密値を公開せず、バックアップ制御用の状態は復元対象から外す。
- 保持期間未決定のため自動削除なし。失敗した途中コピーも調査用に保持。停止は `stopStepwiseBackups`、再有効化は `setupStepwiseBackups`。GASの実行履歴・トリガー失敗通知を確認する。長時間の処理中は書き込みと競合を避けるためロックを取り、通常操作に一時的な待ちが発生し得る。

検証コマンド: `npm run check` / `npm run test:backup-release`。ローカルでは復元コピーの照合、元データ変更時の失敗、秘密設定の破損検出、共有先拒否、再実行、他トリガーの保持、反映元の不一致・暗黙削除の拒否を確認する。Google上での実行時間と実権限は初回接続後に確認する。

## 8. 別チャット・他のAIから参照するとき

- 共通入口は [AGENTS.md](../AGENTS.md)。GitHubの `docs/` を文書の正本とし、Driveの00〜04は対応する正本へのリンクにする。本文を二重保守しない。
- 共通処理は [gas/Code.gs](../gas/Code.gs)、授業記録は [gas/LessonCycle.gs](../gas/LessonCycle.gs)、月間承認・請求は [gas/BillingApproval.gs](../gas/BillingApproval.gs)、授業形式・一括確定は [gas/Scheduling.gs](../gas/Scheduling.gs)、家族・保護者メールは [gas/FamilyPortal.gs](../gas/FamilyPortal.gs)、生徒メールは [gas/StudentEmail.gs](../gas/StudentEmail.gs)、画面は [kanri/index.html](../kanri/index.html) / [yoyaku/index.html](../yoyaku/index.html)。機能を変更するときは対象コードも確認する。
- [Driveの05引き継ぎ](https://drive.google.com/file/d/1N3KMqRqJAC6vT294lFhB1aQEHA3bIMmt/view)は2026-09-07の履歴。過去の編集手段（ClaudeのMonaco操作）、当時の環境やメモリの所在を調べる場合だけ参照する。そこでの「毎回読む順序」「Codexの反映未確認」「v44」「専用検証環境なし」は現行の指示・状態ではない。

## 8-2. 関連ドキュメント

- [MCP_DESIGN.md](MCP_DESIGN.md): ChatGPT / Codex から操作するための MCP サーバー設計と段階計画(2026-09-06〜)
- [MCP_OPERATIONS.md](MCP_OPERATIONS.md): **MCP の運用手順**(置き場所、緊急停止、キー・パスフレーズの変更、再配置、トラブル対応)
- [CONTRACT_CLAUSES_DRAFT.md](CONTRACT_CLAUSES_DRAFT.md): 契約書に追加する条項の下書き(保護者承認・保護者パスワード)
- [PARENT_AUTH.md](PARENT_AUTH.md): 保護者専用認証の実装と反映順序
- [REDESIGN_CURRENT_STATE.md](REDESIGN_CURRENT_STATE.md) / [REDESIGN_MASTER_PLAN.md](REDESIGN_MASTER_PLAN.md) / [LESSON_CYCLE_PHASE1_SPEC.md](LESSON_CYCLE_PHASE1_SPEC.md): 移行前の調査記録・将来計画・授業記録の実装仕様。採否はMASTER_PLAN、具体的な受け入れ条件はPHASE1_SPEC
- MCPサーバー本体は別のprivateリポジトリ `mugilives-max/stepwise-mcp`。接続先・環境・配置手順は [MCP_OPERATIONS.md](MCP_OPERATIONS.md) に集約する。

## 9. 変更履歴(要点)

- 2026-09-09 連絡欄の MCP 処理(取得・処理権・登録の紐づけ・返信と状態の記録)を GAS v54 と stepwise-mcp 0.3.0 へ公開。記録は [6-8節](#6-8-v54連絡欄の-mcp-処理)、仕様は [MCP_OPERATIONS.md 2-3節](MCP_OPERATIONS.md#2-いま使える機能)。

- 2026-09-09 取消申請・成績票PDFと振り返り・連絡欄をGAS v53へ公開。反映結果は [6-7節](#services-release-v53)、仕様は [取消・成績票・連絡欄](#local-learning-services)。

- 2026-09-08 MCP 登録ツール(案内の一括登録・先生の休み・生徒NG/希望の代理登録)を GAS v52 と stepwise-mcp 0.2.0 へ公開。書き込み範囲は `MCP_WRITE_SCOPE`。記録は [6-6節](#6-6-v52mcp-登録ツール)、仕様は [MCP_OPERATIONS.md 2章](MCP_OPERATIONS.md#2-いま使える機能)。

- 2026-09-08 授業記録と月間承認・請求をGAS v46へ公開し、月間計画の途中保存・再送の復旧をv47へ追加公開。実GAS検証・移行・配信確認の記録は [6-1節](#6-1-v46の反映と検証)。

- 2026-09-07 保護者専用認証を本番GAS v45へ反映（実装commit a2b6fe6）。検証・退避の記録は [PARENT_AUTH.md](PARENT_AUTH.md#今回の確認範囲)、残件は [FUTURE_WORK.md](FUTURE_WORK.md)。

- 2026-08-29 予約システム稼働(GAS + Pages)
- 2026-09-04 管理画面 `/kanri/` 追加、生徒管理を管理画面へ移行、高速化
- 2026-09-05 生徒ページをマイページ化(タブ、保護者ページ、希望日程・共有予定・授業できない日、月の授業回数と保護者承認、宿題・テスト予定)、パスワード再設定をメールコード方式に
- 2026-09-05 模試(北辰テスト)の記録を追加(台帳「模試」シート、管理画面・生徒ページで表示、GAS v39)
- 2026-09-06 返事がないまま日付が過ぎた案内を管理画面ホーム/授業/カルテで警告し、「実施済みにする」「未実施」を選べるように(GAS v40)
- 2026-09-06 先生の休み(teacherOff シート)。管理画面から登録、各カレンダーに「休」、案内時に警告、生徒ページにも表示(GAS v41)。同日、ホームの日付タップから登録、時間帯指定(start/end)に対応(GAS v42)
- 2026-09-07 生徒ページを「ホーム」と「予定」の2ページに分割(#home / #schedule)。ホームの予定表には先生の休みを出さない(生徒に不要なため)。commit cb5d51a、GAS 変更なし
- 2026-09-07 管理画面ホームの全体予定表: 生徒の授業できない日を出さない(授業ページには残す)、Google カレンダー風の Box 表示(renderCal の opts.boxes)。授業は時刻順に「時刻 姓 科目」を1件1行で全件表示(省略しない)、時間帯が重なる授業は左の太線でひとまとまり、生徒の希望(緑)・共有予定(ピンク)・先生の休み(灰)も Box。GAS 変更なし
- 2026-09-06 生徒の「授業できない日」も時間帯指定に対応(blocked に start/end)。生徒ページ右上の「トップページへ」リンクを削除(GAS v43)
- 2026-09-06〜07 MCP 連携: GAS に読み取り専用の MCP 入口(v44)、MCP サーバー(閲覧7ツール)を Codex(stdio)と Cloudflare Workers(ChatGPT、OAuth)に配置。運用手順は docs/MCP_OPERATIONS.md


<a id="local-learning-services"></a>
## 取消申請・成績票・連絡欄（2026-09-09、v53公開済み）

本節はv53で公開した追加仕様。公開結果は6-7節を参照。実装の正本は `gas/LearningServices.gs`、共通画面は `assets/learning-services.js`。管理画面ホームに連絡受付件数と受信箱、生徒の個別ページに成績票・連絡、生徒・保護者のページに公開資料・連絡・取消申請を追加した。パネルは開いた時に取得する。

### 取消申請

理由は必須（1,000文字まで）、期限内も含め先生が承認するまで予約枠を維持する。開始24時間前ちょうどまでは通常申請、その後は例外申請。サーバー入口の受付時刻を保存し、排他待ちや先生の確認遅延で区分を変えない。ブラウザ申告時刻は使用しない。病気・大幅な交通遅延等の妥当性は先生が判断する。

申請IDと受付記録を先に保存し、途中失敗時は同じIDで再送できる。管理画面で理由・受付時刻・区分を確認し、既存の取消承認/却下操作で決定する。本人・保護者にも申請結果を表示する。既存の生徒画面からの申請取り下げも記録する。旧申請の履歴を推測で埋めない。取消申請の通知先は管理画面で、新規申請時の先生宛メールは送らない。取消後の既存通知処理は維持する。実施済み・請求・取消料は自動変更しない。

### 成績票と公開範囲

模試/定期テスト、日付、名称、任意PDF（5MiB以下）、振り返り、分析、次回対策を保存できる。点数入力なしで登録でき、従来の数値成績・グラフを残す。保存時に本人と紐付いた保護者へ公開する。非公開欄はその試験の `teacherNote` 1つ。APIで先生以外へ返さず、画面で隠すだけにはしない。

PDFは `EXAM_PDF_FOLDER_ID` の先生専用Driveフォルダへ保存する。未設定なら初回添付時に非公開フォルダを作成する。追加の閲覧者・編集者やリンク共有がある保存先は拒否する。ファイルIDやDriveの公開リンクを画面へ返さず、毎回認証と生徒との関係を検証してPDFを取得する。差し替え前のPDFは非公開で残す。PDF作成後の台帳書き込み失敗は同一内容のファイルを再利用する。試験レコードは版番号による競合検出と同じ処理IDの再送に対応。OCR・点数抽出・旧PDFの自動削除は含まない。

### 連絡欄

認証済みの生徒・保護者が用件（予定/質問/改善要望/その他）と本文（4,000文字まで）を送信する。対象の子どもは認証情報で制約し、原文・送信者・受付時刻を保持する。訂正・補足は `replyTo` で元の連絡を参照する別メッセージ。先生・本人・紐付いた保護者がその子の連絡と返信を共有する。

先生は最新の返信1件と状態を更新できる。状態は `received`（受付済み）、`needs_confirmation`（確認待ち）、`registered`（登録済み）、`failed`（失敗）、`closed`（対応済み）。確認待ち/登録済み/失敗は説明必須。登録済みは先生が実際の予定登録結果を確認して付ける状態で、この操作自体は予定を書き換えない。返信は版番号で競合を検出する。未送信入力はページ内で通信失敗時に保持し、端末への永続保存はしない。ブラウザを閉じる際には未保存警告を出す。

AI による処理は MCP ツール(`list_inbox` → `claim_message` → 登録 → `resolve_message`)で行い、経過は `contactProcessing` シートに残る(v54、2026-09-09)。定期実行は未導入。新しい先生用API(`service*`)はMCPキーを受け付けない。仕様は [MCP_OPERATIONS.md 2-3節](MCP_OPERATIONS.md#2-いま使える機能)、設計は [MCP_DESIGN.md](MCP_DESIGN.md#message-consumer-handoff)。

### 台帳・配置・確認

新規シートは予約台帳の `examReports`、`contactMessages`、`cancellationRequests`。列順の正本は `LearningServices.gs` の `SERVICE_COLS_`。既存列が一致しない場合は書き込みを停止する。読み取りでシートを作らず、初回書き込み時に空シートを準備する。既存データの移動や削除はない。

公開時は2台帳と現行ソースを退避・比較してから、新規シートの列を確認する。新しいGASファイルとCode.gs、共通JSと管理/予約HTMLを一括の反映対象とし、GASを先に反映する。一時的な検証・退避用のエディタファイルは公開前に削除する。既存の `setup()` を再実行しない。Drive権限の追加同意が必要なら先生本人が行い、非公開のテストPDFで実Driveの保存・取得・他生徒からの拒否を確認する。2026-09-09に本人が追加権限を承認し、本番反映と隔離した実Drive検証を実施した。

ローカル検証: 構文検査と410件の自動テストに合格。新機能は認証、家族解除、非公開欄、PDFの再送、返信競合、24時間境界と処理待ち、承認/却下を検証した。隔離したブラウザ画面で成績コメント保存・本人表示、連絡送信・先生の返信・本人側の表示、取消申請を確認。実Driveの非公開保存・認証後取得・他生徒からの拒否は実GASでも確認済み。

### 実施登録と簡潔な授業記録（2026-09-09）

先生の実施済み登録（確定授業・過去の未回答案内）は月間承認・予定回数に依存しない。開始前、定員、処理中、請求発行済み月の保護は維持する。請求発行の料金承認条件は別に維持する。授業記録はやった内容の短い本文を中心とし、補足・詳細項目・先生用メモは任意の折りたたみにする。既存の記録値は保持する。関連96件の回帰テストで確認。

本変更はGAS v57へ反映済み。固定版のソース照合と公開health `2026-09-09-completion-independent` を確認した。台帳変更なし。

### 保護者アカウント統一（2026-09-09）

GAS v58へ反映済み。メールによる保護者アカウントと生徒の紐付け方式に統一し、生徒別パスワードの設定・ログイン・旧セッションを停止。旧URLも統一画面へ案内する。既存家族アカウント・紐付け・業務データは維持。旧認証データは復元用に残す。廃止方式のテストは停止確認へ置き換え、請求・記録の検証を家族方式へ移し、全414件を確認。固定版ソースと公開health `2026-09-09-parent-unified` を照合済み。

### 保護者登録の案内改善（2026-09-09）

GAS v59の公開health `2026-09-09-parent-onboarding` と固定版のソース一致を確認。生徒から保護者追加／既存アカウントへの紐付け、登録リンク、状態別の次の操作、登録途中のパスワード復旧を実装。全417件の隔離テスト通過。業務データ変更なし、実宛先へのメール送信試験は行っていない。手順はPARENT_AUTH.md。

### ローカル差分：生徒グループ・共有ログイン

2026-09-09にローカル実装。公開版はv59のまま。新規生徒の単独グループ、既存グループへの移動、共有アカウントの複数端末ログインを実装。実装仕様はPARENT_AUTH.md。GASソースのhealth識別子にlocalを含め、本番反映済みと混同しない。

検証：全420件に加え、移動途中失敗の再試行と旧単一ハッシュの互換性を追加検証（保護者API36件通過）。本番へのpush・GAS反映・実データ書き込みは行っていない。

### 生徒グループ・共有ログインの公開（2026-09-09）

GAS v60へ反映。退避・v59との基準照合後、固定版ソースの一致と公開health `2026-09-09-student-groups` を確認。切替直後の取得結果が遅れたため、最後は公開版番号と固定版・healthを個別に照合した。公開前の関連54件のテスト通過。既存グループの所属変更や実生徒への書き込みは行っていない。


### v61・保護者のメール確認を先に行う初回登録（2026-09-10）

- 2026-09-10、メールだけを入力→メール確認→パスワード設定→自動ログインへ変更。案内文・再送画面・管理画面の登録段階表示をそろえた。詳細は [PARENT_AUTH.md](PARENT_AUTH.md) を正本とする。
- 台帳の列変更・既存アカウントの一括移行は不要。既存の利用中アカウントと生徒の紐付けを維持する。メール未確認の旧アカウントも、再送したメールからパスワード設定を完了できる。
- ローカル430項目を確認（全体425通過後、旧登録手順に依存した5項目の架空アカウント作成を更新し、関連29項目が通過）。実GASは新規の架空台帳2冊で48項目が通過。メール・Calendarは代替処理で実送信なし。架空台帳2冊と一時検証コードは除去した。
- Chromeのローカル画面で、メールだけの入力・送信先表示・確認待ち→架空の受信リンク→メール確認→パスワード設定→自動ログイン→紐付いた子どもの表示を確認。実メール到着の再試験は追加していない。
- 本番v60とエディタのソースを退避・照合し、FamilyPortalとCodeの2ファイルを更新。固定版v61を作成し既存デプロイIDへ反映。退避・照合・進捗は `.verification/releases/2026-09-10T05-35-07-779Z/`。最終の公開応答・Pages照合は同フォルダに保存する。
- v60へ差し戻す場合、新方式のパスワード未設定アカウントは旧画面で登録を完了できない場合がある。既存授業や台帳全体を巻き戻さず、新規登録を停止して修正版で復旧する。

### v62・認証メールから開く確認画面（2026-09-10）

リンクに対応するメールアドレスを表示し、認証して次へ進む操作に統一。有効なリンクでは再送・ログインボタンを表示しない。表示だけでは認証を完了させず、期限切れ・通信失敗時だけ復旧操作を案内する。仕様はPARENT_AUTH.md。関連65件・構文検査・Chromeで次のパスワード設定画面への遷移を確認。固定版v62と公開healthを照合済み。既存アカウント・業務データの変更や実メール送信は行っていない。退避・反映記録は .verification/releases/2026-09-10T06-40-20-446Z/。

### 生徒の授業可能日時を直接登録に変更（2026-09-11）

生徒画面（ホームの日選択・予定ページの日付入力）から「授業の長さ」の選択と「空き状況を確認」→確認カード→送信の2段階を外し、「登録する」で `wish` / `wishMany` を直接送る。`min` と `availabilitySeen` は送らず、GAS側の既定（60分）で空き状況を判定して記録する。GASの変更・再公開はなし（`wishAvailability` APIは残る）。画面側の確認カード関連コード（wishReview / wishCheck / wishSend）と、その流れ専用のUIテスト5件を削除。保護者ページは同じ `portal.js` を読むため同時に反映。残件: 新規登録の `duration` が60分固定になるため先生の案内フォーム初期値も60分になる（従来は生徒選択の既定90分）。既定値を変える場合はGAS `schedulingWishAvailability_` の既定を要調整。

### 生徒メール通知の項目別オン・オフ（2026-09-11）

生徒の「メール通知」ページに「メールで受け取る項目」を追加し、案内・変更・取消・取消依頼への回答をチェックボックスで個別にオン・オフできるようにした（変更は即保存、失敗時は元に戻す）。GAS は `StudentEmail.gs` のみ変更：`studentEmailPrefs` 操作、`studentEmailStatus_().prefs`、`studentEmailNotify_` でオフの種類を `skipped`（理由「本人の通知設定でオフのため送信しません」）として記録。新シート `studentEmailPrefs` は初回保存時に `ensureSheet_` で作成（既存 `studentEmails` の列は変更しない）。先生の生徒設定にはオフの項目を表示。検証は隔離ハーネスの追加3件（GAS 2件、画面1件）。

### 生徒ホームの予定表にも登録不可を表示（2026-09-11）

2026-09-07 にホームの予定表から外していた先生の休み（現在の表記は「登録不可」）を、先生の依頼でホームにも表示するよう戻した。予定ページと同じ描画（終日は斜線と「登録不可」、時間帯は「登録不可12-13:30」の形、選んだ日の内訳にもチップ）。`portal.js` の呼び出し引数のみの変更で GAS は変更なし。画面テスト1件を追加。

### 生徒ページの「予定」タブを廃止しホームへ統合（2026-09-11）

生徒ページのヘッダー左上を本人名の「〇〇さんのマイページ」（小さく「ステップワイズ個別指導」）に変更（`updateBrand`。保護者ページ・保護者向け表示は従来の教室名のまま）。「予定」タブと `#schedule` を廃止し、予定管理（授業可能日時・予定を共有・授業できない日のボタンと各パネル、日付入力の送信、登録済み一覧）をホームの予定表直下へ移動。日を選ぶモードの案内は予定表の見出しに、入力欄は「選んだ日の予定」カード内の従来どおりの位置に出す。旧リンク `#schedule` はホームとして表示。旧予定ページにあった「今後の予定」一覧はホームでは以前に重複として外していたため復活させていない（授業の取消依頼は予定表の日付から可能）。GAS 変更なし。画面テスト1件追加・1件更新。

### ホームの「予定管理」欄を廃止し、登録・取消を予定表の日付に集約（2026-09-11）

ホームからボタン群「授業可能日時・予定を共有・授業できない日」と各パネル（日付入力フォーム、登録済み一覧）を削除。代わりに「選んだ日の予定」の＋に「予定を共有」を追加し、選んだ日の内訳に授業可能日時（「授業可 16:00〜20:00」＋取消）、授業できない日（解除）、共有した予定（削除）の操作を置いた。予定表には返事待ちの授業可能日時を「授業可」で表示。API の `wish`（単日フォーム）・`block`（期間フォーム）・`eventAdd`（日付入力）は画面からは呼ばなくなったが、GAS 側は残す。将来は自然言語入力からの変換（FUTURE_WORK 参照）で置き換える想定。GAS 変更なし。画面テスト2件を更新。

### 文章からの予定登録（AI 変換、2026-09-11）

生徒ホームの「選んだ日の予定」の＋を押したときに「手動で予定入力」(3ボタン)の下へ「文章で自動入力」を表示（2026-09-11 に別カード「文章で予定を伝える」から統合）。生徒の文章を GAS の `scheduleParse`（`gas/NaturalSchedule.gs`）が Anthropic Messages API（`claude-haiku-4-5-20251001`、ツール呼び出し `propose_schedule` で構造化出力）に渡し、授業できる時間帯（wish）・授業できない日（block）・予定の共有（event、テスト・模試、授業不可を含むか）の候補に変換して返す。画面は候補をチェックボックスで並べ、時間帯が曖昧な wish は時刻入力を出し、「チェックした内容で登録する」で既存の `wishMany` / `blockSet` / `eventAddMany` を順に呼ぶ（保存側の検証はそのまま。登録済みの授業できない日は除外。途中で失敗した項目からやり直せるよう完了項目は「登録済み」にする）。

- 鍵は Script Properties `ANTHROPIC_API_KEY`。先生が Console で発行して自分で設定する。未設定のときは `state.nlEnabled=false` で画面に出さず、API は `notConfigured` を返す。鍵はコード・Git・ログ・応答に含めない。
- API へ送るのは生徒の文章、今日の日付、向こう70日の日付と曜日だけ。氏名・ID・専用リンクは送らない。文章と応答は保存せず、`log` には生徒ID・文字数・状態・トークン数のみ。
- 文章は400文字以内、1人1時間20回まで（CacheService）。失敗時は詳細を返さず、429/529 は「混み合っています」と案内。確定授業の取消は項目にせず質問欄で取消申請へ誘導するよう指示している。
- 正規化（`nlNormalize_`）: 種類外・日付不正・過去・180日超・重複を除外、項目10・日付20の上限、時刻は HH:MM のみ、開始≧終了は破棄、event はタイトル必須40文字、メモ100文字、質問5件。
- 検証: 隔離ハーネス（`test/natural-schedule.test.cjs` 3件: 無効化・認証・境界、送信内容と正規化、失敗と回数制限）と画面テスト2件。実 API の呼び出しは鍵設定後に先生の生徒ページで確認する。
- 費用の目安: 1回あたり入力約1,500トークン・出力約200トークンで 0.3円前後。
- 2026-09-11 の有効化記録: 先生が鍵を設定後、`nlSelfTest` は「You do not have permission to call UrlFetchApp.fetch」で失敗し、エディタの実行では承認画面が出なかった（clasp 経由の公開では新しい権限の承認が求められない）。「デプロイ → デプロイを管理 → 編集 → 新バージョン → デプロイ」で出る「アクセスを承認」から7権限を許可すると `ok items=1` になった。今後 GAS に新しいサービス（UrlFetch・Drive など）を初めて使うコードを入れたときは、公開後にこの手順で先生の承認が必要。
- 疎通確認: エディタで `nlSelfTest` を実行する。初回は UrlFetch(外部接続)の承認画面が出るので先生が許可する（Web アプリは公開後に追加された権限を、所有者が一度承認するまで使えない）。実行ログに `ok items=1` / `http 401 authentication_error`（鍵違い）などが出る。鍵の値は出さない。

### 生徒ホームの「授業登録」を折り畳みに、選択ボタンを1つに（2026-09-11）

案内一覧は `<details>` で既定は閉じた状態（見出しに件数と「返事をお願いします」を表示、開閉状態は再描画をまたいで保持）。「全件選択（31件まで）」「選択を解除」の2ボタンを1つにし、未選択があるときは「一括選択」（`batchall`）、31件までが全部選ばれているときは「選択解除」（`batchclear`）に切り替わる。32件以上あるときだけ「一括選択は31件まで」を添える。GAS 変更なし。画面テスト1件追加。

### 「予定の編集」カードの各項目を行形式に（2026-09-11）

選んだ日の内訳を、授業登録の一覧と同じ `slotline` 行（左に種類のタグ、時刻、内容、右に操作ボタン）に統一。種類: 登録不可（先生の休み）、予定／テスト（削除）、確定（キャンセル）、実施済（開くと公開記録）、授業（過去・未登録）、案内（選択用チェックボックスは授業登録の一括選択と連動、確定・再調整）、× 授業できない（解除）、授業可（返事待ち、取消）。GAS 変更なし。テスト3件の期待値を更新。

### 「授業計画の案内」を独立させ文章で案内（2026-09-11）

今月の科目別回数のカードを授業登録の折り畳みから出し、見出し「授業計画の案内」の下に置いた。保護者の承認待ちなら「9月は 英語4回 の授業計画が届いています。保護者の方に伝えて、保護者ページから承認・調整をお願いしましょう。」、承認済みなら「9月の授業計画は 英語4回 です。」と状態に応じた文章で示し、その下に科目ごとの「実施・予定／計画」と残りの日程調整数を出す。授業登録の折り畳みは案内があるときだけ表示に戻した。GAS 変更なし。画面テスト1件を置き換え。

### やることリストと授業計画の案内も折り畳みに、案内は最下部へ（2026-09-11）

ホームの3節（やることリスト・授業登録・授業計画の案内）を共通の `<details class="fold">`（`foldHead`、開閉状態は `folds` で保持）に統一。やることリストは既定で開、授業登録と授業計画の案内は既定で閉。授業計画の案内の見出しには「保護者の承認待ち／保護者承認済み／N月」を添え、位置は授業登録の下（ホーム最下部）に移動。GAS 変更なし。

### 「授業計画」を案内と実施計画に分割（2026-09-11、GAS `2026-09-11-plan-months`）

`studentState_` に `planMonths`（今月と来月のうち status が proposed / approved で計画のある月。`{ym, status, plan}`）を追加（`plan` / `planStatus` は互換のため維持）。生徒ホーム最下部の折り畳み「授業計画」は、上段「案内」に承認待ちの月の計画を科目ごとの行（案内タグ・月・科目と回数・保護者の承認待ちタグ）で並べ、保護者への依頼文を添える。下段「実施計画」に承認済みの月の計画を行（承認済みタグ・月・実施・予定／計画・あとN回または日程確定）で並べ、残りがあれば日程調整の案内を出す。案内行に選択用の □ は付けていない（生徒側に操作がないため）。テスト: `test/student-plan-months.test.cjs`（GAS 1件）と画面テスト1件を置き換え。

### 授業の種類（通常・演習・講習など）第1段階（2026-09-11、GAS `2026-09-11-lesson-kinds`）

- 新シート `lessonKinds`（name, standardMin, standardFee, active, sortOrder, updatedAt。初期行は「通常」）。`gas/LessonKinds.gs`。管理画面「設定」の「授業の種類」で追加・標準時間・標準料金・有効/無効を編集する（名前は変更しない。「通常」は無効にできない）。標準時間・料金は第2段階（種類別の料金計算）で使う予定で、いまは保存・表示のみ。
- `slots` 13列目・`plans` 11列目に `kind` を追加（`ensureKindColumns_`、schema キャッシュは `schemaOk20`）。空文字は「通常」。承認 JSON（`billingPlanList_`）は通常のとき `kind` を含めないので、既存の承認は現行のまま有効。
- 案内フォーム・案内の編集・月間計画の登録に「種類」の選択（必須、既定は通常）。MCP `offer_lessons` は任意の `kind`（省略時は通常、無効な種類は invalid）。
- 請求前の検査は「科目＋種類」で承認済み計画と突き合わせる。保護者の回数調整（`approvedCounts`）も科目＋種類で対応付ける。
- 表示は「英語（演習）」の形で科目に種類を添える（`kindLabel_` / 画面の `lessonLabel`）。生徒の授業計画・授業登録・予定表、管理画面の一覧、保護者の計画表示に反映。成績・授業の記録・宿題の期限は科目単位のまま。
- テスト: `test/lesson-kinds.test.cjs`（マスタ、計画・案内・MCP・生徒状態・請求前検査）。

### 生徒の予定表の表記を整理（2026-09-11）

「共有した予定」と「テスト・模試」の見た目を1つにし、凡例と選んだ日の行では「重要な予定」と表示（内部の `kind=test` はテストまでの日数表示のために維持し、予定共有の「テスト・模試」チェックも残す）。生徒の「授業できない日」は × をやめ、先生の休みと同じ斜線（赤みの薄い色）＋「授業不可」のラベルに変更。時間帯だけの授業不可は「授業不可16-18」の形。日を選ぶモードの印も同じ文言に統一。GAS 変更なし。テスト2件を更新。

### 生徒ページ: タブ「メール通知」を「設定」に、ページ見出しを整理（2026-09-11）

「メール通知」タブを「設定」に改名し（ルートと `#student-email?verify=` のリンクは従来どおり）、ページ内を「設定 > メール通知」の見出しに。成績票・振り返り／先生への連絡の欄は「成績」「授業の記録」から外して「設定」タブにだけ表示。成績ページと授業の記録ページの「〇〇さんの成績」「〇〇さんの授業の記録」の見出しを削除（説明文は残す）。GAS 変更なし。

### 授業の記録を科目＋種類のフォルダ表示に（2026-09-11）

生徒の「授業の記録」を「月ごと」から、科目＋種類（例: 英語、英語（講習））ごとのフォルダに変更。一覧は管理画面の生徒一覧と同じカード（名前・回数・分・最終日・記録件数）で、タップするとそのフォルダを開き「← 一覧に戻る」で戻る。中は日付の新しい順に、公開された授業記録（開くと内容・宿題）と、記録がまだ無い実施済みの授業（行）を並べる。授業記録の種類は同じ日時・科目の授業から引く。フォルダが1つだけなら開いた状態。`renderPublishedRecords` の1件描画を `publishedRecordItem` に分離（保護者ページの記録一覧は従来どおり）。GAS 変更なし。画面テスト1件追加。

### 授業記録を「単元」と「コメント」に簡素化（2026-09-11）

先生の記録入力は「単元」（`report.actualUnit`）と「コメント（必須）」（`content`）を前面に出し、取り組みの様子・次回の焦点・先生用メモ・その他の報告項目は「補足・先生用メモ（任意）」の折り畳みに残した（データ構造・API・既存記録は変更なし）。生徒・保護者に見せる授業記録は日付・科目・単元を見出しに、開くと「単元」「コメント」と宿題だけを表示（保護者向けには「保護者への連絡」があれば追加）。同じ科目の月間予定などの補足情報や取り組みの様子・次回の焦点は表示しない。ホームの日付カードの見出しも「授業の内容」から「コメント」に統一。`lesson-report.js` の `fields` に除外指定を追加し、読み込みにキャッシュ用の版を付けた。GAS 変更なし。

### 「登録不可」に説明ボタン（2026-09-11）

生徒ホームの「予定の編集」で登録不可（先生の休み）の行の右に「？」ボタンを付け、押すと「先生の予定があるため、この時間帯には授業を登録できません。別の日時を選ぶか、先生にご相談ください。」を表示（もう一度押すと閉じる）。GAS 変更なし。

### 取消・解除・削除の前に確認バー（2026-09-11）

生徒ホームの「予定の編集」で、授業できない日の「解除」、授業できる時間帯の「取消」、重要な予定の「削除」は、押すとすぐ送信せず、画面下の確認バー（既存の `pending` の仕組みに `kind: remove` を追加）に「〜を解除しますか?」と対象の日時を出し、「解除する／やめる」で確定する。GAS 変更なし。テスト3件を更新。

### 予定表の授業に終了時刻を表示（2026-09-11）

生徒の予定表の授業ラベルを「17:00」から「17:00-18:30」（開始-終了）に変更。案内・確定・実施済みとも同じ。重要な予定は従来どおり内容のみ。GAS 変更なし。

### 予定表の授業を1コマ1箱で表示（2026-09-11）

生徒の予定表で授業・案内・重要な予定をそれぞれ1つの箱（左に色の帯。確定・実施済み＝青、案内＝黄、重要な予定＝赤系）で表示し、1行目に時刻「17:00-18:30」、2行目に科目。1日3件まで表示し、超える分は「+N」。凡例は従来のまま（実施済みを灰色にする案は見づらいため取りやめ）。管理画面ホームの箱表示と同じ考え方。GAS 変更なし。
